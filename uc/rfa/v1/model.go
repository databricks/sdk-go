// Code generated by Databricks SDK Generator. DO NOT EDIT.

package rfa

import (
	"github.com/databricks/sdk-go/core/types"
)

type DestinationType string

const (
	DestinationType_Unspecified    DestinationType = ""
	DestinationType_Email          DestinationType = "EMAIL"
	DestinationType_Slack          DestinationType = "SLACK"
	DestinationType_GenericWebhook DestinationType = "GENERIC_WEBHOOK"
	DestinationType_MicrosoftTeams DestinationType = "MICROSOFT_TEAMS"
	DestinationType_Url            DestinationType = "URL"
)

type PrincipalType string

const (
	PrincipalType_Unspecified      PrincipalType = ""
	PrincipalType_UserPrincipal    PrincipalType = "USER_PRINCIPAL"
	PrincipalType_GroupPrincipal   PrincipalType = "GROUP_PRINCIPAL"
	PrincipalType_ServicePrincipal PrincipalType = "SERVICE_PRINCIPAL"
)

// The type of Unity Catalog securable.
type SecurableType string

const (
	SecurableType_Unspecified       SecurableType = ""
	SecurableType_Catalog           SecurableType = "CATALOG"
	SecurableType_Schema            SecurableType = "SCHEMA"
	SecurableType_Table             SecurableType = "TABLE"
	SecurableType_StorageCredential SecurableType = "STORAGE_CREDENTIAL"
	SecurableType_ExternalLocation  SecurableType = "EXTERNAL_LOCATION"
	SecurableType_Function          SecurableType = "FUNCTION"
	SecurableType_Share             SecurableType = "SHARE"
	SecurableType_Provider          SecurableType = "PROVIDER"
	SecurableType_Recipient         SecurableType = "RECIPIENT"
	SecurableType_CleanRoom         SecurableType = "CLEAN_ROOM"
	SecurableType_Metastore         SecurableType = "METASTORE"
	SecurableType_Pipeline          SecurableType = "PIPELINE"
	SecurableType_Volume            SecurableType = "VOLUME"
	SecurableType_Connection        SecurableType = "CONNECTION"
	SecurableType_Credential        SecurableType = "CREDENTIAL"
	SecurableType_ExternalMetadata  SecurableType = "EXTERNAL_METADATA"
	// TODO: [UC-2980] Staging tables aren't full-fleged securables yet.
	SecurableType_StagingTable         SecurableType = "STAGING_TABLE"
	SecurableType_Model                SecurableType = "MODEL"
	SecurableType_ModelService         SecurableType = "MODEL_SERVICE"
	SecurableType_McpService           SecurableType = "MCP_SERVICE"
	SecurableType_ModelProviderService SecurableType = "MODEL_PROVIDER_SERVICE"
)

type SpecialDestination string

const (
	SpecialDestination_Unspecified                             SpecialDestination = ""
	SpecialDestination_SpecialDestinationCatalogOwner          SpecialDestination = "SPECIAL_DESTINATION_CATALOG_OWNER"
	SpecialDestination_SpecialDestinationExternalLocationOwner SpecialDestination = "SPECIAL_DESTINATION_EXTERNAL_LOCATION_OWNER"
	SpecialDestination_SpecialDestinationConnectionOwner       SpecialDestination = "SPECIAL_DESTINATION_CONNECTION_OWNER"
	SpecialDestination_SpecialDestinationCredentialOwner       SpecialDestination = "SPECIAL_DESTINATION_CREDENTIAL_OWNER"
	SpecialDestination_SpecialDestinationMetastoreOwner        SpecialDestination = "SPECIAL_DESTINATION_METASTORE_OWNER"
	SpecialDestination_SpecialDestinationSchemaOwner           SpecialDestination = "SPECIAL_DESTINATION_SCHEMA_OWNER"
	SpecialDestination_SpecialDestinationTableOwner            SpecialDestination = "SPECIAL_DESTINATION_TABLE_OWNER"
	SpecialDestination_SpecialDestinationVolumeOwner           SpecialDestination = "SPECIAL_DESTINATION_VOLUME_OWNER"
	SpecialDestination_SpecialDestinationFunctionOwner         SpecialDestination = "SPECIAL_DESTINATION_FUNCTION_OWNER"
	SpecialDestination_SpecialDestinationRegisteredModelOwner  SpecialDestination = "SPECIAL_DESTINATION_REGISTERED_MODEL_OWNER"
)

type AccessRequestDestinations struct {
	// The access request destinations for the securable.
	Destinations []NotificationDestination `fieldmask:"destinations"`
	// The securable for which the access request destinations are being modified or
	// read.
	Securable *Securable `fieldmask:"securable"`
	// Indicates whether any destinations are hidden from the caller due to a lack
	// of permissions. This value is true if the caller does not have permission to
	// see all destinations.
	AreAnyDestinationsHidden *bool `fieldmask:"are_any_destinations_hidden"`
	// The source securable from which the destinations are inherited. Either the
	// same value as securable (if destination is set directly on the securable) or
	// the nearest parent securable with destinations set.
	DestinationSourceSecurable *Securable `fieldmask:"destination_source_securable"`
	// The type of the securable. Redundant with the type in the securable object,
	// but necessary for Terraform integration
	SecurableType *string `fieldmask:"securable_type"`
	// The full name of the securable. Redundant with the name in the securable
	// object, but necessary for Terraform integration
	FullName *string `fieldmask:"full_name"`
}

type BatchCreateAccessRequestsRequest struct {
	// A list of individual access requests, where each request corresponds to a set
	// of permissions being requested on a list of securables for a specified
	// principal.
	//
	// At most 30 requests per API call.
	Requests []CreateAccessRequest
}

type BatchCreateAccessRequestsResponse struct {
	// The access request destinations for each securable object the principal
	// requested.
	Responses []CreateAccessRequestResponse
}

type CreateAccessRequest struct {
	// Optional. The principal this request is for. Empty `behalf_of` defaults to
	// the requester's identity.
	//
	// Principals must be unique across the API call.
	BehalfOf *Principal
	// Optional. Comment associated with the request.
	//
	// At most 200 characters, can only contain lowercase/uppercase letters (a-z,
	// A-Z), numbers (0-9), punctuation, and spaces.
	Comment *string
	// List of securables and their corresponding requested UC privileges.
	//
	// At most 30 securables can be requested for a principal per batched call. Each
	// securable can only be requested once per principal.
	SecurablePermissions []SecurablePermissions
}

type CreateAccessRequestResponse struct {
	// The principal the request was made on behalf of.
	BehalfOf *Principal
	// The access request destinations for all the securables the principal
	// requested.
	RequestDestinations []AccessRequestDestinations
}

type GetAccessRequestDestinationsRequest struct {
	// The type of the securable.
	SecurableType *string
	// The full name of the securable.
	FullName *string
}

type NotificationDestination struct {
	// The identifier for the destination. This is the email address for EMAIL
	// destinations, the URL for URL destinations, or the unique <Databricks>
	// notification destination ID for all other external destinations.
	DestinationId *string
	// The type of the destination.
	DestinationType DestinationType
	// This field is used to denote whether the destination is the email of the
	// owner of the securable object. The special destination cannot be assigned to
	// a securable and only represents the default destination of the securable. The
	// securable types that support default special destinations are: "catalog",
	// "external_location", "connection", "credential", and "metastore". The
	// **destination_type** of a **special_destination** is always EMAIL.
	SpecialDestination SpecialDestination
}

type Principal struct {
	// <Databricks> user, group or service principal ID.
	Id            *string
	PrincipalType PrincipalType
}

// Generic definition of a securable, which is uniquely defined in a metastore
// by its type and full name..
type Securable struct {
	// Required. The type of securable (catalog/schema/table). Optional if
	// resource_name is present.
	Type SecurableType `fieldmask:"type"`
	// Required. The full name of the catalog/schema/table. Optional if
	// resource_name is present.
	FullName *string `fieldmask:"full_name"`
	// Optional. The name of the Share object that contains the securable when the
	// securable is getting shared in D2D Delta Sharing.
	ProviderShare *string `fieldmask:"provider_share"`
}

type SecurablePermissions struct {
	// The securable for which the access request destinations are being requested.
	Securable *Securable
	// List of requested Unity Catalog permissions.
	Permissions []string
}

type UpdateAccessRequestDestinationsRequest struct {
	// The access request destinations to assign to the securable. For each
	// destination, a **destination_id** and **destination_type** must be defined.
	AccessRequestDestinations *AccessRequestDestinations
	UpdateMask                *types.FieldMask[AccessRequestDestinations]
}
