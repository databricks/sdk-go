// Code generated by Databricks SDK Generator. DO NOT EDIT.

package dataquality

import (
	"bytes"
	"context"
	"encoding/json"
	"iter"
	"log/slog"
	"net/http"
	"net/url"
	"sync"

	"github.com/databricks/sdk-go/auth"
	"github.com/databricks/sdk-go/core/clientinfo"
	"github.com/databricks/sdk-go/dataquality/internal"
	"github.com/databricks/sdk-go/options/call"
	"github.com/databricks/sdk-go/options/client"
	"github.com/databricks/sdk-go/options/internaloptions"
)

type Client struct {
	internalClient

	// extensions is a per-client store for hand-written mixins to attach
	// lazily-built state. Keys should be unexported types private to the
	// mixin, following the context.Value convention, to avoid collisions.
	// The zero value is ready to use; it is safe for concurrent use.
	extensions sync.Map
}

type internalClient struct {
	httpClient  *http.Client
	credentials auth.Credentials
	logger      *slog.Logger
	userAgent   string
	host        string
	workspaceID string
	accountID   string
}

func NewClient(ctx context.Context, opts ...client.Option) (*Client, error) {
	cfg := internaloptions.ClientOptions{}
	for _, opt := range opts {
		if err := opt(&cfg); err != nil {
			return nil, err
		}
	}
	if err := cfg.Resolve(); err != nil {
		return nil, err
	}
	info, err := clientinfo.Default().With(
		internal.ModuleName, internal.Version,
		"auth", cfg.Credentials.Name(),
	)
	if err != nil {
		return nil, err
	}

	return &Client{
		internalClient: internalClient{
			httpClient:  cfg.HTTPClient,
			credentials: cfg.Credentials,
			logger:      cfg.Logger,
			userAgent:   info.String(),
			host:        cfg.Host,
			workspaceID: cfg.WorkspaceID,
			accountID:   cfg.AccountID,
		},
	}, nil
}

// Cancels a data quality monitor refresh. Currently only supported for the
// `table` `object_type`. The call must be made in the same workspace as where
// the monitor was created.
//
// The caller must have either of the following sets of permissions: 1.
// **MANAGE** and **USE_CATALOG** on the table's parent catalog. 2.
// **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
// **MANAGE** on the table.
func (c *internalClient) CancelRefresh(ctx context.Context, req *CancelRefreshRequest, opts ...call.Option) (*CancelRefreshResponse, error) {
	body, err := json.Marshal(cancelRefreshRequestToWire(req))
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	pb.literal("/refreshes/")
	pb.singleSegment(*req.RefreshId)
	pb.literal("/cancel")
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *CancelRefreshResponse

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "POST",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp cancelRefreshResponseWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = cancelRefreshResponseFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Create a data quality monitor on a Unity Catalog object. The caller must
// provide either `anomaly_detection_config` for a schema monitor or
// `data_profiling_config` for a table monitor.
//
// For the `table` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
// catalog, **USE_SCHEMA** on the table's parent schema, and **SELECT** on the
// table 2. **USE_CATALOG** on the table's parent catalog, **MANAGE** and
// **USE_SCHEMA** on the table's parent schema, and **SELECT** on the table. 3.
// **USE_CATALOG** on the table's parent catalog, **USE_SCHEMA** on the table's
// parent schema, and **MANAGE** and **SELECT** on the table.
//
// Workspace assets, such as the dashboard, will be created in the workspace
// where this call was made.
//
// For the `schema` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and **MANAGE**
// and **USE_SCHEMA** on the schema.
func (c *internalClient) CreateMonitor(ctx context.Context, req *CreateMonitorRequest, opts ...call.Option) (*Monitor, error) {
	body, err := json.Marshal(monitorToWire(req.Monitor))
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/data-quality/v1/monitors"
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *Monitor

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "POST",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp monitorWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = monitorFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Creates a refresh. Currently only supported for the `table` `object_type`.
// The call must be made in the same workspace as where the monitor was created.
//
// The caller must have either of the following sets of permissions: 1.
// **MANAGE** and **USE_CATALOG** on the table's parent catalog. 2.
// **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
// **MANAGE** on the table.
func (c *internalClient) CreateRefresh(ctx context.Context, req *CreateRefreshRequest, opts ...call.Option) (*Refresh, error) {
	body, err := json.Marshal(refreshToWire(req.Refresh))
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.Refresh.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.Refresh.ObjectId)
	pb.literal("/refreshes")
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *Refresh

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "POST",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp refreshWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = refreshFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Delete a data quality monitor on Unity Catalog object.
//
// For the `table` `object_type`, the caller must have either of the following
// sets of permissions: **MANAGE** and **USE_CATALOG** on the table's parent
// catalog. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. **USE_CATALOG** on the table's
// parent catalog, **USE_SCHEMA** on the table's parent schema, and **MANAGE**
// on the table.
//
// Note that the metric tables and dashboard will not be deleted as part of this
// call; those assets must be manually cleaned up (if desired).
//
// For the `schema` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and **MANAGE**
// and **USE_SCHEMA** on the schema.
func (c *internalClient) DeleteMonitor(ctx context.Context, req *DeleteMonitorRequest, opts ...call.Option) error {

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "DELETE",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		_ = respBody
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return err
	}
	return nil
}

// (Unimplemented) Delete a refresh
func (c *internalClient) DeleteRefresh(ctx context.Context, req *DeleteRefreshRequest, opts ...call.Option) error {

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	pb.literal("/refreshes/")
	pb.singleSegment(*req.RefreshId)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "DELETE",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		_ = respBody
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return err
	}
	return nil
}

// Read a data quality monitor on a Unity Catalog object.
//
// For the `table` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
// **SELECT** on the table.
//
// For the `schema` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and
// **USE_SCHEMA** on the schema.
//
// The returned information includes configuration values on the entity and
// parent entity as well as information on assets created by the monitor. Some
// information (e.g. dashboard) may be filtered out if the caller is in a
// different workspace than where the monitor was created.
func (c *internalClient) GetMonitor(ctx context.Context, req *GetMonitorRequest, opts ...call.Option) (*Monitor, error) {

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *Monitor

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp monitorWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = monitorFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Get data quality monitor refresh. The call must be made in the same workspace
// as where the monitor was created.
//
// For the `table` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
// **SELECT** on the table.
//
// For the `schema` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and
// **USE_SCHEMA** on the schema.
func (c *internalClient) GetRefresh(ctx context.Context, req *GetRefreshRequest, opts ...call.Option) (*Refresh, error) {

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	pb.literal("/refreshes/")
	pb.singleSegment(*req.RefreshId)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *Refresh

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp refreshWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = refreshFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// (Unimplemented) List data quality monitors.
func (c *internalClient) ListMonitor(ctx context.Context, req *ListMonitorRequest, opts ...call.Option) (*ListMonitorResponse, error) {

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/data-quality/v1/monitors"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "page_token", req.PageToken); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_size", req.PageSize); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ListMonitorResponse

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp listMonitorResponseWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = listMonitorResponseFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// ListMonitorIter returns an iterator that iterates
// over the results of ListMonitor.
//
// For example:
//
//	for item, err := range c.ListMonitorIter(ctx, &ListMonitorRequest{}) {
//	  if err != nil {
//	    return err
//	  }
//	  fmt.Println(item)
//	}
//
// Options opts are passed to each ListMonitor call
// made by the iterator under the hood.
//
// Callers who need custom pagination logic should use
// ListMonitor directly.
func (c *internalClient) ListMonitorIter(ctx context.Context, req *ListMonitorRequest, opts ...call.Option) iter.Seq2[*Monitor, error] {
	return func(yield func(*Monitor, error) bool) {
		// Deep copy the request via JSON round-trip to avoid modifying the original.
		reqBody, err := json.Marshal(req)
		if err != nil {
			yield(nil, err)
			return
		}
		pageReq := ListMonitorRequest{}
		if err := json.Unmarshal(reqBody, &pageReq); err != nil {
			yield(nil, err)
			return
		}
		for {
			resp, err := c.ListMonitor(ctx, &pageReq, opts...)
			if err != nil {
				yield(nil, err)
				return
			}
			for i := range resp.Monitors {
				if !yield(&resp.Monitors[i], nil) {
					return
				}
			}
			if resp.NextPageToken == nil || *resp.NextPageToken == "" {
				return
			}
			pageReq.PageToken = resp.NextPageToken
		}
	}
}

// List data quality monitor refreshes. The call must be made in the same
// workspace as where the monitor was created.
//
// For the `table` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
// **SELECT** on the table.
//
// For the `schema` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and
// **USE_SCHEMA** on the schema.
func (c *internalClient) ListRefresh(ctx context.Context, req *ListRefreshRequest, opts ...call.Option) (*ListRefreshResponse, error) {

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	pb.literal("/refreshes")
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "page_token", req.PageToken); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_size", req.PageSize); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ListRefreshResponse

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp listRefreshResponseWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = listRefreshResponseFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// ListRefreshIter returns an iterator that iterates
// over the results of ListRefresh.
//
// For example:
//
//	for item, err := range c.ListRefreshIter(ctx, &ListRefreshRequest{}) {
//	  if err != nil {
//	    return err
//	  }
//	  fmt.Println(item)
//	}
//
// Options opts are passed to each ListRefresh call
// made by the iterator under the hood.
//
// Callers who need custom pagination logic should use
// ListRefresh directly.
func (c *internalClient) ListRefreshIter(ctx context.Context, req *ListRefreshRequest, opts ...call.Option) iter.Seq2[*Refresh, error] {
	return func(yield func(*Refresh, error) bool) {
		// Deep copy the request via JSON round-trip to avoid modifying the original.
		reqBody, err := json.Marshal(req)
		if err != nil {
			yield(nil, err)
			return
		}
		pageReq := ListRefreshRequest{}
		if err := json.Unmarshal(reqBody, &pageReq); err != nil {
			yield(nil, err)
			return
		}
		for {
			resp, err := c.ListRefresh(ctx, &pageReq, opts...)
			if err != nil {
				yield(nil, err)
				return
			}
			for i := range resp.Refreshes {
				if !yield(&resp.Refreshes[i], nil) {
					return
				}
			}
			if resp.NextPageToken == nil || *resp.NextPageToken == "" {
				return
			}
			pageReq.PageToken = resp.NextPageToken
		}
	}
}

// Update a data quality monitor on Unity Catalog object.
//
// For the `table` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
// **MANAGE** on the table.
//
// For the `schema` `object_type`, the caller must have either of the following
// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and **MANAGE**
// and **USE_SCHEMA** on the schema.
func (c *internalClient) UpdateMonitor(ctx context.Context, req *UpdateMonitorRequest, opts ...call.Option) (*Monitor, error) {
	body, err := json.Marshal(monitorToWire(req.Monitor))
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "update_mask", req.UpdateMask); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *Monitor

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "PATCH",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp monitorWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = monitorFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// (Unimplemented) Update a refresh
func (c *internalClient) UpdateRefresh(ctx context.Context, req *UpdateRefreshRequest, opts ...call.Option) (*Refresh, error) {
	body, err := json.Marshal(refreshToWire(req.Refresh))
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	if c.userAgent != "" {
		headers.Set("User-Agent", c.userAgent)
	}
	headers.Set("Content-Type", "application/json")

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/data-quality/v1/monitors/")
	pb.singleSegment(*req.ObjectType)
	pb.literal("/")
	pb.singleSegment(*req.ObjectId)
	pb.literal("/refreshes/")
	pb.singleSegment(*req.RefreshId)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "update_mask", req.UpdateMask); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *Refresh

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "PATCH",
			URL:         urlStr,
			Credentials: c.credentials,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp refreshWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp = refreshFromWire(&wireResp)
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}
