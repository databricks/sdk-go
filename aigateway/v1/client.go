// Code generated by Databricks SDK Generator. DO NOT EDIT.

package aigateway

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"iter"
	"log/slog"
	"net/http"
	"net/url"
	"sync"

	"github.com/databricks/sdk-go/aigateway/internal"
	"github.com/databricks/sdk-go/auth"
	"github.com/databricks/sdk-go/core/clientinfo"
	"github.com/databricks/sdk-go/options/call"
	"github.com/databricks/sdk-go/options/client"
	"github.com/databricks/sdk-go/options/internaloptions"
)

type Client struct {
	internalClient

	// extensions is a per-client store for hand-written mixins to attach
	// lazily-built state. Keys should be unexported types private to the
	// mixin, following the context.Value convention, to avoid collisions.
	// The zero value is ready to use; it is safe for concurrent use.
	extensions sync.Map
}

type internalClient struct {
	httpClient  *http.Client
	credentials auth.Credentials
	logger      *slog.Logger
	userAgent   func() (string, error)
	host        string
	workspaceID string
	accountID   string
}

func NewClient(ctx context.Context, opts ...client.Option) (*Client, error) {
	cfg := internaloptions.ClientOptions{}
	for _, opt := range opts {
		if err := opt(&cfg); err != nil {
			return nil, err
		}
	}
	if err := cfg.Resolve(); err != nil {
		return nil, err
	}
	userAgent := func() (string, error) {
		info, err := clientinfo.Default().With(
			internal.ModuleName, internal.Version,
			"auth", cfg.Credentials.Name(),
		)
		if err != nil {
			return "", err
		}
		return info.String(), nil
	}

	return &Client{
		internalClient: internalClient{
			httpClient:  cfg.HTTPClient,
			credentials: cfg.Credentials,
			logger:      cfg.Logger,
			userAgent:   userAgent,
			host:        cfg.Host,
			workspaceID: cfg.WorkspaceID,
			accountID:   cfg.AccountID,
		},
	}, nil
}

// Creates an MCP service in a Unity Catalog schema. An MCP (Model Context
// Protocol) service is a governed securable that registers an MCP server and
// exposes its tools for discovery, access control, and invocation. Specify its
// name in `mcp_service_id`.
//
// You must be the owner of the parent schema or have the `CREATE_SERVICE` and
// `USE_SCHEMA` privileges on the parent schema and `USE_CATALOG` on the parent
// catalog. You also need `USE_CONNECTION` on the connection the MCP service
// references.
func (c *internalClient) CreateMcpService(ctx context.Context, req CreateMcpServiceRequest, opts ...call.Option) (*McpService, error) {
	wireReq, err := createMcpServiceRequestToWire(&req)
	if err != nil {
		return nil, err
	}
	body, err := json.Marshal(wireReq.McpService)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/2.1/unity-catalog/mcp-services"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "parent", wireReq.Parent); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "mcp_service_id", wireReq.McpServiceId); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *McpService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "POST",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp mcpServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = mcpServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Creates a model provider service in a Unity Catalog schema. A model provider
// service stores authentication and request configuration for an external model
// provider, such as OpenAI, Azure OpenAI, or Amazon Bedrock. Model services
// reference it to invoke the provider. Specify its name in
// `model_provider_service_id`.
//
// You must be the owner of the parent schema or have the `CREATE_SERVICE` and
// `USE_SCHEMA` privileges on the parent schema and `USE_CATALOG` on the parent
// catalog. Inline credentials additionally require `CREATE_CONNECTION` on the
// parent schema. When using a Unity Catalog service credential, you must have
// `ACCESS` on that credential.
func (c *internalClient) CreateModelProviderService(ctx context.Context, req CreateModelProviderServiceRequest, opts ...call.Option) (*ModelProviderService, error) {
	wireReq, err := createModelProviderServiceRequestToWire(&req)
	if err != nil {
		return nil, err
	}
	body, err := json.Marshal(wireReq.ModelProviderService)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/2.1/unity-catalog/model-provider-services"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "parent", wireReq.Parent); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "model_provider_service_id", wireReq.ModelProviderServiceId); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ModelProviderService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "POST",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp modelProviderServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = modelProviderServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Creates a model service in a Unity Catalog schema. A model service is a
// governed AI Gateway endpoint that routes inference requests to one or more
// model destinations. Specify its name in `model_service_id`.
//
// You must be the owner of the parent schema or have the `CREATE_SERVICE` and
// `USE_SCHEMA` privileges on the parent schema and `USE_CATALOG` on the parent
// catalog. For every destination, you also need `USE_CATALOG` and `USE_SCHEMA`
// on its parent and `EXECUTE` on the referenced Unity Catalog model or model
// provider service. A provisioned-throughput destination additionally requires
// `CAN_MANAGE` on its Model Serving endpoint. Configuring an inference table
// additionally requires `CREATE_TABLE`.
func (c *internalClient) CreateModelService(ctx context.Context, req CreateModelServiceRequest, opts ...call.Option) (*ModelService, error) {
	wireReq, err := createModelServiceRequestToWire(&req)
	if err != nil {
		return nil, err
	}
	body, err := json.Marshal(wireReq.ModelService)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/2.1/unity-catalog/model-services"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "parent", wireReq.Parent); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "model_service_id", wireReq.ModelServiceId); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ModelService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "POST",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp modelServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = modelServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Deletes the MCP service identified by its resource name. Optionally supply an
// `etag` to make the delete conditional on the MCP service not having changed
// since it was read.
//
// You must be the owner of the MCP service or have `MANAGE` on it, plus
// `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent schema.
func (c *internalClient) DeleteMcpService(ctx context.Context, req DeleteMcpServiceRequest, opts ...call.Option) error {
	wireReq, err := deleteMcpServiceRequestToWire(&req)
	if err != nil {
		return err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.Name == nil {
		return fmt.Errorf("path parameter %q is required", "name")
	}
	pb.singleSegment(*req.Name)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "etag", wireReq.Etag); err != nil {
		return err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "DELETE",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		_ = respBody
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return err
	}
	return nil
}

// Deletes the model provider service identified by its resource name.
// Optionally supply an `etag` to make the delete conditional on the model
// provider service not having changed since it was read.
//
// You must be the owner of the model provider service or have `MANAGE` on it,
// plus `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent
// schema.
func (c *internalClient) DeleteModelProviderService(ctx context.Context, req DeleteModelProviderServiceRequest, opts ...call.Option) error {
	wireReq, err := deleteModelProviderServiceRequestToWire(&req)
	if err != nil {
		return err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.Name == nil {
		return fmt.Errorf("path parameter %q is required", "name")
	}
	pb.singleSegment(*req.Name)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "etag", wireReq.Etag); err != nil {
		return err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "DELETE",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		_ = respBody
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return err
	}
	return nil
}

// Deletes the model service identified by its resource name. Optionally supply
// an `etag` to make the delete conditional on the model service not having
// changed since it was read.
//
// You must be the owner of the model service or have `MANAGE` on it, plus
// `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent schema.
func (c *internalClient) DeleteModelService(ctx context.Context, req DeleteModelServiceRequest, opts ...call.Option) error {
	wireReq, err := deleteModelServiceRequestToWire(&req)
	if err != nil {
		return err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.Name == nil {
		return fmt.Errorf("path parameter %q is required", "name")
	}
	pb.singleSegment(*req.Name)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "etag", wireReq.Etag); err != nil {
		return err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "DELETE",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		_ = respBody
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return err
	}
	return nil
}

// Returns the MCP service identified by its resource name.
//
// You must be the owner of the MCP service or have `EXECUTE`, `READ_METADATA`,
// or `MANAGE` on it, plus `USE_CATALOG` on the parent catalog and `USE_SCHEMA`
// on the parent schema.
func (c *internalClient) GetMcpService(ctx context.Context, req GetMcpServiceRequest, opts ...call.Option) (*McpService, error) {

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.Name == nil {
		return nil, fmt.Errorf("path parameter %q is required", "name")
	}
	pb.singleSegment(*req.Name)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *McpService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp mcpServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = mcpServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Returns the model provider service identified by its resource name.
//
// You must be the owner of the model provider service or have `EXECUTE`,
// `READ_METADATA`, or `MANAGE` on it, plus `USE_CATALOG` on the parent catalog
// and `USE_SCHEMA` on the parent schema.
func (c *internalClient) GetModelProviderService(ctx context.Context, req GetModelProviderServiceRequest, opts ...call.Option) (*ModelProviderService, error) {

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.Name == nil {
		return nil, fmt.Errorf("path parameter %q is required", "name")
	}
	pb.singleSegment(*req.Name)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ModelProviderService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp modelProviderServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = modelProviderServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Returns the model service identified by its resource name.
//
// You must be the owner of the model service or have `EXECUTE`,
// `READ_METADATA`, or `MANAGE` on it, plus `USE_CATALOG` on the parent catalog
// and `USE_SCHEMA` on the parent schema.
func (c *internalClient) GetModelService(ctx context.Context, req GetModelServiceRequest, opts ...call.Option) (*ModelService, error) {

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.Name == nil {
		return nil, fmt.Errorf("path parameter %q is required", "name")
	}
	pb.singleSegment(*req.Name)
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ModelService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp modelServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = modelServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Lists the MCP services in a Unity Catalog schema. Provide `parent` as
// `schemas/{catalog}.{schema}`. Results are paginated; pass the returned
// `next_page_token` to fetch subsequent pages.
//
// Requires `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent
// schema. Only MCP services the caller can access (as owner or through
// `EXECUTE`, `READ_METADATA`, or `MANAGE`) are returned.
func (c *internalClient) ListMcpServices(ctx context.Context, req ListMcpServicesRequest, opts ...call.Option) (*ListMcpServicesResponse, error) {
	wireReq, err := listMcpServicesRequestToWire(&req)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/2.1/unity-catalog/mcp-services"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "parent", wireReq.Parent); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_size", wireReq.PageSize); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_token", wireReq.PageToken); err != nil {
		return nil, err
	}
	if wireReq.View != "" {
		if err := addQueryValue(queryParams, "view", wireReq.View); err != nil {
			return nil, err
		}
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ListMcpServicesResponse

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp listMcpServicesResponseWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = listMcpServicesResponseFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// ListMcpServicesIter returns an iterator that iterates
// over the results of ListMcpServices.
//
// For example:
//
//	for item, err := range c.ListMcpServicesIter(ctx, ListMcpServicesRequest{}) {
//	  if err != nil {
//	    return err
//	  }
//	  fmt.Println(item)
//	}
//
// Options opts are passed to each ListMcpServices call
// made by the iterator under the hood.
//
// Callers who need custom pagination logic should use
// ListMcpServices directly.
func (c *internalClient) ListMcpServicesIter(ctx context.Context, req ListMcpServicesRequest, opts ...call.Option) iter.Seq2[*McpService, error] {
	return func(yield func(*McpService, error) bool) {
		// Keep pagination state local to this traversal so reusing the iterator starts
		// from the original request. Reference-bearing fields remain shared and read-only.
		pageReq := req
		for {
			resp, err := c.ListMcpServices(ctx, pageReq, opts...)
			if err != nil {
				yield(nil, err)
				return
			}
			for i := range resp.McpServices {
				if !yield(&resp.McpServices[i], nil) {
					return
				}
			}
			if resp.NextPageToken == nil || *resp.NextPageToken == "" {
				return
			}
			pageReq.PageToken = resp.NextPageToken
		}
	}
}

// Lists the model provider services in a Unity Catalog schema. Provide `parent`
// as `schemas/{catalog}.{schema}`. Results are paginated; pass the returned
// `next_page_token` to fetch subsequent pages.
//
// Requires `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent
// schema. Only model provider services the caller can access (as owner or
// through `EXECUTE`, `READ_METADATA`, or `MANAGE`) are returned.
func (c *internalClient) ListModelProviderServices(ctx context.Context, req ListModelProviderServicesRequest, opts ...call.Option) (*ListModelProviderServicesResponse, error) {
	wireReq, err := listModelProviderServicesRequestToWire(&req)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/2.1/unity-catalog/model-provider-services"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "parent", wireReq.Parent); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_size", wireReq.PageSize); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_token", wireReq.PageToken); err != nil {
		return nil, err
	}
	if wireReq.View != "" {
		if err := addQueryValue(queryParams, "view", wireReq.View); err != nil {
			return nil, err
		}
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ListModelProviderServicesResponse

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp listModelProviderServicesResponseWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = listModelProviderServicesResponseFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// ListModelProviderServicesIter returns an iterator that iterates
// over the results of ListModelProviderServices.
//
// For example:
//
//	for item, err := range c.ListModelProviderServicesIter(ctx, ListModelProviderServicesRequest{}) {
//	  if err != nil {
//	    return err
//	  }
//	  fmt.Println(item)
//	}
//
// Options opts are passed to each ListModelProviderServices call
// made by the iterator under the hood.
//
// Callers who need custom pagination logic should use
// ListModelProviderServices directly.
func (c *internalClient) ListModelProviderServicesIter(ctx context.Context, req ListModelProviderServicesRequest, opts ...call.Option) iter.Seq2[*ModelProviderService, error] {
	return func(yield func(*ModelProviderService, error) bool) {
		// Keep pagination state local to this traversal so reusing the iterator starts
		// from the original request. Reference-bearing fields remain shared and read-only.
		pageReq := req
		for {
			resp, err := c.ListModelProviderServices(ctx, pageReq, opts...)
			if err != nil {
				yield(nil, err)
				return
			}
			for i := range resp.ModelProviderServices {
				if !yield(&resp.ModelProviderServices[i], nil) {
					return
				}
			}
			if resp.NextPageToken == nil || *resp.NextPageToken == "" {
				return
			}
			pageReq.PageToken = resp.NextPageToken
		}
	}
}

// Lists the model services in a Unity Catalog schema. Provide `parent` as
// `schemas/{catalog}.{schema}`. Results are paginated; pass the returned
// `next_page_token` to fetch subsequent pages.
//
// Requires `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent
// schema. Only model services the caller can access (as owner or through
// `EXECUTE`, `READ_METADATA`, or `MANAGE`) are returned.
func (c *internalClient) ListModelServices(ctx context.Context, req ListModelServicesRequest, opts ...call.Option) (*ListModelServicesResponse, error) {
	wireReq, err := listModelServicesRequestToWire(&req)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	baseURL.Path = "/api/2.1/unity-catalog/model-services"
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "parent", wireReq.Parent); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_size", wireReq.PageSize); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "page_token", wireReq.PageToken); err != nil {
		return nil, err
	}
	if wireReq.View != "" {
		if err := addQueryValue(queryParams, "view", wireReq.View); err != nil {
			return nil, err
		}
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ListModelServicesResponse

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "GET",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp listModelServicesResponseWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = listModelServicesResponseFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// ListModelServicesIter returns an iterator that iterates
// over the results of ListModelServices.
//
// For example:
//
//	for item, err := range c.ListModelServicesIter(ctx, ListModelServicesRequest{}) {
//	  if err != nil {
//	    return err
//	  }
//	  fmt.Println(item)
//	}
//
// Options opts are passed to each ListModelServices call
// made by the iterator under the hood.
//
// Callers who need custom pagination logic should use
// ListModelServices directly.
func (c *internalClient) ListModelServicesIter(ctx context.Context, req ListModelServicesRequest, opts ...call.Option) iter.Seq2[*ModelService, error] {
	return func(yield func(*ModelService, error) bool) {
		// Keep pagination state local to this traversal so reusing the iterator starts
		// from the original request. Reference-bearing fields remain shared and read-only.
		pageReq := req
		for {
			resp, err := c.ListModelServices(ctx, pageReq, opts...)
			if err != nil {
				yield(nil, err)
				return
			}
			for i := range resp.ModelServices {
				if !yield(&resp.ModelServices[i], nil) {
					return
				}
			}
			if resp.NextPageToken == nil || *resp.NextPageToken == "" {
				return
			}
			pageReq.PageToken = resp.NextPageToken
		}
	}
}

// Updates an MCP service. Only the fields named in `update_mask` are changed;
// the resource name is immutable. Optionally supply an `etag` to make the
// update conditional on the MCP service not having changed since it was read.
//
// You must be the owner of the MCP service or have `MANAGE` on it, plus
// `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent schema.
// When changing `config.source_connection.name`, the MCP service owner must
// also have `USE_CONNECTION` on the new connection.
func (c *internalClient) UpdateMcpService(ctx context.Context, req UpdateMcpServiceRequest, opts ...call.Option) (*McpService, error) {
	wireReq, err := updateMcpServiceRequestToWire(&req)
	if err != nil {
		return nil, err
	}
	body, err := json.Marshal(wireReq.McpService)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.McpService == nil || req.McpService.Name == nil {
		pb.singleSegment("")
	} else {
		pb.singleSegment(*req.McpService.Name)
	}
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "update_mask", wireReq.UpdateMask); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "etag", wireReq.Etag); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *McpService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "PATCH",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp mcpServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = mcpServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Updates a model provider service. Only the fields named in `update_mask` are
// changed; the resource name and provider type are immutable. Optionally supply
// an `etag` to make the update conditional on the model provider service not
// having changed since it was read.
//
// You must be the owner of the model provider service or have `MANAGE` on it,
// plus `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent
// schema.
//
// Updating `config.provider` cannot change the provider type or switch between
// Unity Catalog service-credential authentication and inline authentication.
func (c *internalClient) UpdateModelProviderService(ctx context.Context, req UpdateModelProviderServiceRequest, opts ...call.Option) (*ModelProviderService, error) {
	wireReq, err := updateModelProviderServiceRequestToWire(&req)
	if err != nil {
		return nil, err
	}
	body, err := json.Marshal(wireReq.ModelProviderService)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.ModelProviderService == nil || req.ModelProviderService.Name == nil {
		pb.singleSegment("")
	} else {
		pb.singleSegment(*req.ModelProviderService.Name)
	}
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "update_mask", wireReq.UpdateMask); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "etag", wireReq.Etag); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ModelProviderService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "PATCH",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp modelProviderServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = modelProviderServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}

// Updates a model service. Only the fields named in `update_mask` are changed;
// the resource name is immutable. Optionally supply an `etag` to make the
// update conditional on the model service not having changed since it was read.
//
// You must be the owner of the model service or have `MANAGE` on it, plus
// `USE_CATALOG` on the parent catalog and `USE_SCHEMA` on the parent schema.
// When changing destinations, both you and the model service owner need
// `USE_CATALOG` and `USE_SCHEMA` on each destination's parent and `EXECUTE` on
// the referenced Unity Catalog model or model provider service. A
// provisioned-throughput destination additionally requires `CAN_MANAGE` for you
// and `CAN_QUERY` for the model service owner. Adding an inference table
// additionally requires `CREATE_TABLE`.
func (c *internalClient) UpdateModelService(ctx context.Context, req UpdateModelServiceRequest, opts ...call.Option) (*ModelService, error) {
	wireReq, err := updateModelServiceRequestToWire(&req)
	if err != nil {
		return nil, err
	}
	body, err := json.Marshal(wireReq.ModelService)
	if err != nil {
		return nil, err
	}

	headers := http.Header{}
	headers.Set("Content-Type", "application/json")
	if c.workspaceID != "" {
		headers.Set("X-Databricks-Workspace-Id", c.workspaceID)
	}

	baseURL, err := url.Parse(c.host)
	if err != nil {
		return nil, err
	}
	pb := pathBuilder{}
	pb.literal("/api/2.1/unity-catalog/")
	if req.ModelService == nil || req.ModelService.Name == nil {
		pb.singleSegment("")
	} else {
		pb.singleSegment(*req.ModelService.Name)
	}
	baseURL.Path, baseURL.RawPath = pb.build()
	queryParams := url.Values{}
	if err := addQueryValue(queryParams, "update_mask", wireReq.UpdateMask); err != nil {
		return nil, err
	}
	if err := addQueryValue(queryParams, "etag", wireReq.Etag); err != nil {
		return nil, err
	}
	baseURL.RawQuery = queryParams.Encode()
	urlStr := baseURL.String()

	var resp *ModelService

	call := func(ctx context.Context) error {
		httpReq, err := newHTTPRequest(ctx, httpRequestOptions{
			Method:      "PATCH",
			URL:         urlStr,
			Credentials: c.credentials,
			UserAgent:   c.userAgent,
			Headers:     headers,
			Body:        bytes.NewBuffer(body),
		})
		if err != nil {
			return err
		}

		respBody, _, err := executeHTTPCall(httpCallOptions{
			req:    httpReq,
			client: c.httpClient,
			logger: c.logger,
		})
		if err != nil {
			return err
		}
		var wireResp modelServiceWire
		if err := json.Unmarshal(respBody, &wireResp); err != nil {
			return err
		}
		resp, err = modelServiceFromWire(&wireResp)
		if err != nil {
			return err
		}
		return nil
	}

	if err := executeCall(ctx, call, opts); err != nil {
		return nil, err
	}
	return resp, nil
}
