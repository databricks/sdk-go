// Code generated by Databricks SDK Generator. DO NOT EDIT.

package aigateway

import (
	"github.com/databricks/sdk-go/core/types"
)

// Controls which fields are populated on each McpService in the response. The
// server uses `BASIC` when `view` is unset. Callers needing the full
// configuration must request it explicitly with `view = FULL`.
type ListMcpServicesRequest_View string

const (
	ListMcpServicesRequest_View_Unspecified ListMcpServicesRequest_View = ""
	// All fields populated, including the fully resolved `config` (connection
	// details) and rate-limit principal names.
	ListMcpServicesRequest_View_Full ListMcpServicesRequest_View = "FULL"
	// Basic resource metadata and selected configuration fields. Source connection
	// details and rate-limit principal names are unset.
	ListMcpServicesRequest_View_Basic ListMcpServicesRequest_View = "BASIC"
)

// Controls which fields are populated on each ModelProviderService in the
// response. The server uses `BASIC` when `view` is unset. Callers needing the
// full configuration must request it explicitly with `view = FULL`.
type ListModelProviderServicesRequest_View string

const (
	ListModelProviderServicesRequest_View_Unspecified ListModelProviderServicesRequest_View = ""
	// All fields populated, including resolved service-credential and
	// inference-table details and rate-limit principal names.
	ListModelProviderServicesRequest_View_Full ListModelProviderServicesRequest_View = "FULL"
	// Basic resource metadata and selected configuration fields. Service-credential
	// and inference-table details and rate-limit principal names are unset.
	ListModelProviderServicesRequest_View_Basic ListModelProviderServicesRequest_View = "BASIC"
)

// Controls which fields are populated on each ModelService in the response. The
// server uses `BASIC` when `view` is unset. Callers needing the full
// configuration must request it explicitly with `view = FULL`.
type ListModelServicesRequest_View string

const (
	ListModelServicesRequest_View_Unspecified ListModelServicesRequest_View = ""
	// All fields populated, including the fully resolved `config` (destinations and
	// inference-table details) and rate-limit principal names.
	ListModelServicesRequest_View_Full ListModelServicesRequest_View = "FULL"
	// Basic resource metadata and selected configuration fields. Destinations and
	// inference-table details are unset.
	ListModelServicesRequest_View_Basic ListModelServicesRequest_View = "BASIC"
)

// External LLM provider for an EXTERNAL_FOUNDATION_MODEL destination.
type ModelProviderServiceConfig_ExternalModelProviderType string

const (
	ModelProviderServiceConfig_ExternalModelProviderType_Unspecified ModelProviderServiceConfig_ExternalModelProviderType = ""
	// OpenAI (api.openai.com). Auth via API key.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeOpenai ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_OPENAI"
	// Azure OpenAI Service. Authentication uses an API key, an Entra ID service
	// principal, or a Unity Catalog service credential.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeAzureOpenai ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_AZURE_OPENAI"
	// Anthropic (api.anthropic.com). Authentication uses an API key or the caller's
	// relayed OAuth token.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeAnthropic ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_ANTHROPIC"
	// Amazon Bedrock. Authentication uses an AWS access-key pair or a Unity Catalog
	// service credential.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeAmazonBedrock ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_AMAZON_BEDROCK"
	// Custom OpenAI-compatible provider (any endpoint that speaks the OpenAI HTTP
	// API). Configured by `base_url` + API key.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeCustom ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_CUSTOM"
	// Microsoft Foundry. Configure a Foundry endpoint URL and authentication
	// credentials.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeMicrosoftFoundry ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_MICROSOFT_FOUNDRY"
	// Google Gemini Enterprise. Auth via API key.
	ModelProviderServiceConfig_ExternalModelProviderType_ExternalModelProviderTypeGeminiEnterprise ModelProviderServiceConfig_ExternalModelProviderType = "EXTERNAL_MODEL_PROVIDER_TYPE_GEMINI_ENTERPRISE"
)

// Backing-model category for a model service destination.
type ModelServiceConfig_DestinationConfig_DestinationType string

const (
	ModelServiceConfig_DestinationConfig_DestinationType_Unspecified ModelServiceConfig_DestinationConfig_DestinationType = ""
	// A <Databricks> foundation model billed per token.
	ModelServiceConfig_DestinationConfig_DestinationType_DestinationTypePayPerTokenFoundationModel ModelServiceConfig_DestinationConfig_DestinationType = "DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL"
	// A <Databricks> foundation model with provisioned throughput.
	ModelServiceConfig_DestinationConfig_DestinationType_DestinationTypeProvisionedThroughputFoundationModel ModelServiceConfig_DestinationConfig_DestinationType = "DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL"
	// An external LLM provider (OpenAI, Anthropic, Azure OpenAI, Bedrock, ...).
	ModelServiceConfig_DestinationConfig_DestinationType_DestinationTypeExternalFoundationModel ModelServiceConfig_DestinationConfig_DestinationType = "DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL"
)

// Scope key for a rate limit.
type RateLimit_RateLimitKey string

const (
	RateLimit_RateLimitKey_Unspecified RateLimit_RateLimitKey = ""
	// Rate limit applies to a specific user (matched on `principal`).
	RateLimit_RateLimitKey_RateLimitKeyUser RateLimit_RateLimitKey = "RATE_LIMIT_KEY_USER"
	// Rate limit applies to all members of a group (matched on `principal`).
	RateLimit_RateLimitKey_RateLimitKeyUserGroup RateLimit_RateLimitKey = "RATE_LIMIT_KEY_USER_GROUP"
	// Rate limit applies to a specific service principal (matched on `principal`).
	RateLimit_RateLimitKey_RateLimitKeyServicePrincipal RateLimit_RateLimitKey = "RATE_LIMIT_KEY_SERVICE_PRINCIPAL"
	// Rate limit applies to the parent service (ModelService or McpService) as a
	// whole, across all callers. Domain-neutral so the same enum can scope a
	// service-wide quota on either securable.
	RateLimit_RateLimitKey_RateLimitKeyService RateLimit_RateLimitKey = "RATE_LIMIT_KEY_SERVICE"
	// Default per-user rate limit applied when no more-specific rule matches.
	RateLimit_RateLimitKey_RateLimitKeyUserDefault RateLimit_RateLimitKey = "RATE_LIMIT_KEY_USER_DEFAULT"
)

// Renewal period for a rate limit.
type RateLimit_RateLimitRenewalPeriod string

const (
	RateLimit_RateLimitRenewalPeriod_Unspecified RateLimit_RateLimitRenewalPeriod = ""
	// Rate limit counters reset every minute.
	RateLimit_RateLimitRenewalPeriod_RateLimitRenewalPeriodMinute RateLimit_RateLimitRenewalPeriod = "RATE_LIMIT_RENEWAL_PERIOD_MINUTE"
	// Rate limit counters reset every hour.
	RateLimit_RateLimitRenewalPeriod_RateLimitRenewalPeriodHour RateLimit_RateLimitRenewalPeriod = "RATE_LIMIT_RENEWAL_PERIOD_HOUR"
)

// Request to create a new MCP service..
type CreateMcpServiceRequest struct {
	// Name of the parent schema. Format: `schemas/{catalog}.{schema}`. Each `{...}`
	// component is capped at 255 characters individually.
	Parent *string
	// Name for the MCP service, e.g. "my_mcp_service".
	McpServiceId *string
	// The MCP service to create. Do not set `name`; the server derives it from
	// `parent` and `mcp_service_id`. `source_connection` is required.
	McpService *McpService
}

// Request to create a new model provider service..
type CreateModelProviderServiceRequest struct {
	// Name of the parent schema. Format: `schemas/{catalog}.{schema}`. Each `{...}`
	// component is capped at 255 characters individually.
	Parent *string
	// Name for the model provider service, e.g. "openai_prod".
	ModelProviderServiceId *string
	// The model provider service to create. Do not set `name`; the server derives
	// it from `parent` and `model_provider_service_id`.
	ModelProviderService *ModelProviderService
}

// Request to create a new model service..
type CreateModelServiceRequest struct {
	// Name of the parent schema. Format: `schemas/{catalog}.{schema}`. Each `{...}`
	// component is capped at 255 characters individually.
	Parent *string
	// Name for the model service, e.g. "my_model_service".
	ModelServiceId *string
	// The model service to create. Do not set `name`; the server derives it from
	// `parent` and `model_service_id`.
	ModelService *ModelService
}

// Request to delete an MCP service..
type DeleteMcpServiceRequest struct {
	// Resource name of the MCP service. Format:
	// `mcp-services/{catalog}.{schema}.{mcp_service}`. Each `{...}` component is
	// capped at 255 characters individually.
	Name *string
	// Optimistic concurrency token from the most recent read. When set, the delete
	// succeeds only if the resource has not changed. Leave unset for an
	// unconditional delete. For REST requests, URL-encode the base64 string
	// returned by the API when setting the `etag` query parameter.
	Etag []byte
}

// Request to delete a model provider service..
type DeleteModelProviderServiceRequest struct {
	// Resource name of the model provider service. Format:
	// `model-provider-services/{catalog}.{schema}.{model_provider_service}`. Each
	// `{...}` component is capped at 255 characters individually.
	Name *string
	// Optimistic concurrency token from the most recent read. When set, the delete
	// succeeds only if the resource has not changed. Leave unset for an
	// unconditional delete. For REST requests, URL-encode the base64 string
	// returned by the API when setting the `etag` query parameter.
	Etag []byte
}

// Request to delete a model service..
type DeleteModelServiceRequest struct {
	// Resource name of the model service. Format:
	// `model-services/{catalog}.{schema}.{model_service}`. Each `{...}` component
	// is capped at 255 characters individually.
	Name *string
	// Optimistic concurrency token from the most recent read. When set, the delete
	// succeeds only if the resource has not changed. Leave unset for an
	// unconditional delete. For REST requests, URL-encode the base64 string
	// returned by the API when setting the `etag` query parameter.
	Etag []byte
}

// Request to get an MCP service..
type GetMcpServiceRequest struct {
	// Resource name of the MCP service. Format:
	// `mcp-services/{catalog}.{schema}.{mcp_service}`. Each `{...}` component is
	// capped at 255 characters individually.
	Name *string
}

// Request to get a model provider service..
type GetModelProviderServiceRequest struct {
	// Resource name of the model provider service. Format:
	// `model-provider-services/{catalog}.{schema}.{model_provider_service}`. Each
	// `{...}` component is capped at 255 characters individually.
	Name *string
}

// Request to get a model service..
type GetModelServiceRequest struct {
	// Resource name of the model service. Format:
	// `model-services/{catalog}.{schema}.{model_service}`. Each `{...}` component
	// is capped at 255 characters individually.
	Name *string
}

// Configuration for logging request and response payloads to a Unity Catalog
// inference table. When this configuration is present, payload logging is
// enabled by default..
type InferenceTableConfig struct {
	// Parent Unity Catalog schema where the inference table is created, in the form
	// `schemas/{catalog}.{schema}`. Required when configuring an inference table.
	// After the inference table is created, this field cannot be changed.
	Parent *string `fieldmask:"parent"`
	// Prefix used to form the inference table's registered name. AI Gateway appends
	// `_payload`; for example, `table_name_prefix = "orders"` creates
	// `orders_payload`. If unset, the prefix defaults to the service name. Read
	// `table` from the response for the resulting resource name. After the
	// inference table is created, this field cannot be changed.
	TableNamePrefix *string `fieldmask:"table_name_prefix"`
	// Resolved UC table for payload logs. Format:
	// `tables/{catalog}.{schema}.{table}`.
	Table *string `fieldmask:"table"`
	// Whether the referenced inference table has been deleted. The configuration
	// remains visible so you can identify the broken dependency. Payload logging
	// cannot continue until the table is restored or the configuration is updated.
	IsDeleted *bool `fieldmask:"is_deleted"`
}

// Request to list MCP services. Accepts `parent`, `page_size`, `page_token`,
// and `view`..
type ListMcpServicesRequest struct {
	// Parent schema to list within, in the form `schemas/{catalog}.{schema}`.
	// Required. Each `{...}` component is capped at 255 characters individually.
	Parent *string
	// Maximum number of MCP services to return. Defaults to 100 when unset or 0;
	// the maximum is 100. Use `page_token` to retrieve additional pages.
	PageSize *int
	// Opaque pagination token from the previous response.
	PageToken *string
	// Fields to return for each service. `FULL` includes source-connection details
	// and rate-limit principal names. `BASIC` omits the source connection and omits
	// principal names from rate limits. Defaults to `BASIC` when unset.
	View ListMcpServicesRequest_View
}

// Response for listing MCP services..
type ListMcpServicesResponse struct {
	// The list of MCP services.
	McpServices []McpService
	// Pagination token for retrieving the next page. Empty when there are no more
	// results.
	NextPageToken *string
}

// Request to list model provider services. Accepts `parent`, `page_size`,
// `page_token`, and `view`..
type ListModelProviderServicesRequest struct {
	// Parent schema to list within, in the form `schemas/{catalog}.{schema}`.
	// Required. Each `{...}` component is capped at 255 characters individually.
	Parent *string
	// Maximum number of provider services to return. Defaults to 100 when unset or
	// 0; the maximum is 100. Use `page_token` to retrieve additional pages.
	PageSize *int
	// Opaque pagination token from the previous response.
	PageToken *string
	// Fields to return for each service. `FULL` includes resolved
	// service-credential and inference-table details and rate-limit principal
	// names. `BASIC` omits those details and principal names from rate limits.
	// Defaults to `BASIC` when unset.
	View ListModelProviderServicesRequest_View
}

// Response for listing model provider services..
type ListModelProviderServicesResponse struct {
	// The list of model provider services.
	ModelProviderServices []ModelProviderService
	// Pagination token for retrieving the next page. Empty when there are no more
	// results.
	NextPageToken *string
}

// Request to list model services. Accepts `parent`, `page_size`, `page_token`,
// and `view`..
type ListModelServicesRequest struct {
	// Parent schema to list within, in the form `schemas/{catalog}.{schema}`.
	// Required. Each `{...}` component is capped at 255 characters individually.
	Parent *string
	// Maximum number of model services to return. Defaults to 100 when unset or 0;
	// the maximum is 100. Use `page_token` to retrieve additional pages.
	PageSize *int
	// Opaque pagination token from the previous response.
	PageToken *string
	// Fields to return for each service. `FULL` includes destinations,
	// inference-table details, and rate-limit principal names. `BASIC` omits
	// destinations and inference-table details and omits principal names from rate
	// limits. Defaults to `BASIC` when unset.
	View ListModelServicesRequest_View
}

// Response for listing model services..
type ListModelServicesResponse struct {
	// The list of model services.
	ModelServices []ModelService
	// Pagination token for retrieving the next page. Empty when there are no more
	// results.
	NextPageToken *string
}

// A Unity Catalog securable that registers an MCP server through a Unity
// Catalog connection and exposes its tools for discovery, authorization, and
// invocation..
type McpService struct {
	// Resource name of the MCP service. Format:
	// `mcp-services/{catalog}.{schema}.{mcp_service}`. Each `{...}` component is
	// capped at 255 characters individually. Server-derived on Create from `parent`
	// + `mcp_service_id`; required and immutable on Update/Get/Delete.
	Name *string `fieldmask:"name"`
	// Owner of the MCP service.
	EffectiveOwner *string `fieldmask:"effective_owner"`
	// Metastore hosting the MCP service.
	MetastoreId *string `fieldmask:"metastore_id"`
	// Time the MCP service was created.
	CreateTime *types.Time `fieldmask:"create_time"`
	// Creator identity.
	CreatedBy *string `fieldmask:"created_by"`
	// Time the MCP service was last modified.
	UpdateTime *types.Time `fieldmask:"update_time"`
	// Identity of the last updater.
	UpdatedBy *string `fieldmask:"updated_by"`
	// User-provided description.
	Comment *string `fieldmask:"comment"`
	// Connection, tool selectors, and rate limits. Required on Create. On Update,
	// provide this field when `update_mask` contains `config` or one of its
	// subpaths.
	Config *McpServiceConfig `fieldmask:"config"`
	// Optimistic concurrency token returned on every read. To make an Update or
	// Delete conditional, pass the last-read value in that request's `etag` field.
	// In REST responses, this value is a base64 string; URL-encode it when setting
	// the `etag` query parameter.
	Etag []byte `fieldmask:"etag"`
}

// Operational configuration for an MCP service. Groups the source reference,
// tool selectors, and rate limits -- the fields that configure how the MCP
// service behaves at invocation time..
type McpServiceConfig struct {
	// Polymorphic reference to where the MCP server lives. MCP_SERVICE is a
	// single-kind securable (`MCP_SERVICE_STANDARD`) with two source variants:
	// `source_connection` (a UC Connection FQN) and `internal` (a
	// <Databricks>-hosted MCP server). (-- Future MANAGED variants (if introduced)
	// would slot additional oneof entries here. --)
	//
	// JSON shape: the active oneof variant appears as a sibling field on `config`
	// (proto JSON does not nest the oneof container name). E.g.: { "config": {
	// "source_connection": {"name": "connections/main.default.gh"},
	// "include_tool_selectors": ["read_*"], ... } } { "config": { "internal":
	// {"server": "sandbox"}, ... } } Future variants slot in the same way:
	// `{"config": {"app": {...}, ...}}`, `{"config": {"genie": {...}, ...}}`, etc.
	// (-- The oneof shape lets future kinds add type-specific reference shapes
	// without a wire-format bump. --)
	Source isMcpServiceConfig_Source
	// Tool names or prefix patterns to expose from the MCP server. Use exact tool
	// names or prefix patterns such as `read_*`. An empty list exposes all tools.
	// At most 1,024 selectors are allowed, and each selector can contain at most
	// 256 characters.
	IncludeToolSelectors []string `fieldmask:"include_tool_selectors"`
	// Rate limits for tool invocations. Supported scopes are user, group, service
	// principal, the service as a whole, and each user by default. Request and
	// token limits are supported. Empty when no rate limit is configured.
	RateLimits []RateLimit                                `fieldmask:"rate_limits"`
	_          [0]mcpServiceConfigSourceFieldMaskMetadata `fieldmask_oneof:"Source"`
}

type isMcpServiceConfig_Source interface {
	isMcpServiceConfig_Source()
}

// McpServiceConfig_Source_SourceConnection selects SourceConnection for McpServiceConfig.Source.
// Unity Catalog connection referencing the MCP server. Required on Create.
type McpServiceConfig_Source_SourceConnection struct {
	SourceConnection McpServiceConfig_SourceConnection `fieldmask:"source_connection"`
}

func (*McpServiceConfig_Source_SourceConnection) isMcpServiceConfig_Source() {}

type mcpServiceConfigSourceFieldMaskMetadata struct {
	*McpServiceConfig_Source_SourceConnection
}

// Unity Catalog connection that points to the MCP server. On Create, provide
// `name` in the schema-scoped form
// `connections/{catalog}.{schema}.{connection}`. On read, the service populates
// the resolved connection metadata. If the connection is deleted, its reference
// remains visible so you can identify the broken dependency..
type McpServiceConfig_SourceConnection struct {
	// Resource name of the Unity Catalog connection used to access the MCP server,
	// in the form `connections/{catalog}.{schema}.{connection}`.
	Name *string `fieldmask:"name"`
	// Whether the referenced connection has been deleted. The MCP service keeps the
	// reference so callers can identify the broken dependency; tool invocation
	// fails until the source connection is updated.
	IsDeleted *bool `fieldmask:"is_deleted"`
}

// A Unity Catalog securable that stores authentication and request
// configuration for an external model provider, such as OpenAI, Azure OpenAI,
// or Amazon Bedrock. Model service destinations reference it to send requests
// to that provider.
//
// A model provider service can be referenced by multiple model services. A
// model service can route across multiple model provider services for traffic
// splitting or failover..
type ModelProviderService struct {
	// Resource name of the provider service. Format:
	// `model-provider-services/{catalog}.{schema}.{model_provider_service}`. Each
	// `{...}` component is capped at 255 characters individually. Server-derived on
	// Create from `parent` + `model_provider_service_id`; required and immutable on
	// Update/Get/Delete.
	Name *string `fieldmask:"name"`
	// Owner of the model provider service.
	EffectiveOwner *string `fieldmask:"effective_owner"`
	// Metastore hosting the provider service.
	MetastoreId *string `fieldmask:"metastore_id"`
	// Time the provider service was created.
	CreateTime *types.Time `fieldmask:"create_time"`
	// Creator identity.
	CreatedBy *string `fieldmask:"created_by"`
	// Time the provider service was last modified.
	UpdateTime *types.Time `fieldmask:"update_time"`
	// Identity of the last updater.
	UpdatedBy *string `fieldmask:"updated_by"`
	// User-provided description.
	Comment *string `fieldmask:"comment"`
	// Optimistic concurrency token returned on every read. To make an Update or
	// Delete conditional, pass the last-read value in that request's `etag` field.
	// In REST responses, this value is a base64 string; URL-encode it when setting
	// the `etag` query parameter.
	Etag []byte `fieldmask:"etag"`
	// Provider authentication, exposed models, request-forwarding controls, rate
	// limits, and payload logging. Required on Create. On Update, it is required
	// only when `config` or one of its subpaths appears in `update_mask`.
	Config *ModelProviderServiceConfig `fieldmask:"config"`
}

// Behavioral configuration for a ModelProviderService: provider authentication
// and provider-specific fields, the catalog of models this provider service can
// route to, and the passthrough policy that governs how request headers, query
// parameters, and unmanaged subpaths cross the trust boundary to the upstream
// provider..
type ModelProviderServiceConfig struct {
	// External model provider. Required on Create and immutable thereafter. Set the
	// matching provider-specific configuration, such as `openai`, `azure_openai`,
	// or `amazon_bedrock`.
	ProviderType ModelProviderServiceConfig_ExternalModelProviderType `fieldmask:"provider_type"`
	// Provider-specific configuration. Exactly one variant must be set, and it must
	// match `provider_type`. Supply secret values in the nested `plaintext` field.
	// Secret values are stored encrypted and omitted from responses; non-secret
	// configuration fields are returned as stored.
	//
	// (-- Declarative tooling must treat `plaintext` as write-only because it never
	// round-trips on reads. --)
	//
	// (-- Secrets are persisted as the encrypted credential of a per-MPS UC
	// SchemaConnection (`CONNECTION_HTTP_BEARER`). The auto-minted SchemaConnection
	// is user-owned but hidden from the user, so it is not surfaced as a connection
	// they manage directly even though the credential could in principle be changed
	// out of band. --)
	//
	// (-- Field-behavior on the per-provider config fields:
	// `(google.api.field_behavior) = OPTIONAL` everywhere on the *DirectConfig
	// descendants, with Create-time requirements enforced in the validator.
	// Proto-level REQUIRED is deliberately not used per the
	// `proto-required-vs-update-mask` guardrail: REQUIRED would reject sparse
	// Update requests that legitimately omit a field whose value is unchanged,
	// breaking AIP-134 partial-Update. The user-facing javadoc on each field states
	// which fields are required on Create. --)
	Provider isModelProviderServiceConfig_Provider
	// When true, accepts any model exposed by the upstream provider; `targets` is
	// not required and does not restrict routability. When false, only models
	// listed in `targets` are routable. Defaults to false.
	AllowAllTargets *bool `fieldmask:"allow_all_targets"`
	// Models and provider-native API types exposed by this provider service. Each
	// entry must include at least one `native_api_types` value. When
	// `allow_all_targets` is false, at least one entry is required and model
	// service destinations can reference only listed models. When
	// `allow_all_targets` is true, any upstream model is routable; entries in this
	// list provide API-type metadata without restricting other models.
	Targets []ModelProviderServiceConfig_ModelTargetConfig `fieldmask:"targets"`
	// Whether to forward incoming HTTP headers to the upstream provider. Defaults
	// to false and is configured for the entire provider service, not per request.
	// Upstream authentication is configured separately in the provider-specific
	// configuration.
	ForwardHeaders *bool `fieldmask:"forward_headers"`
	// Whether to forward incoming query parameters to the upstream provider.
	// Defaults to false and is configured for the entire provider service, not per
	// request.
	ForwardQueryParameters *bool `fieldmask:"forward_query_parameters"`
	// Whether to proxy paths that AI Gateway does not recognize as configured
	// provider-native API types. Defaults to false. When true, these paths are
	// forwarded unchanged to the upstream provider. When false, only recognized API
	// paths are served. Enabling this broadens the upstream API surface exposed
	// through the provider service.
	ForwardUnmanagedPaths *bool `fieldmask:"forward_unmanaged_paths"`
	// Rate limits for requests sent directly to this provider service. Requests
	// routed through a model service use that model service's rate limits instead.
	RateLimits []RateLimit `fieldmask:"rate_limits"`
	// Payload logging configuration for requests sent directly to this provider
	// service. Requests routed through a model service are captured by that model
	// service's inference table instead.
	InferenceTable *InferenceTableConfig                                  `fieldmask:"inference_table"`
	_              [0]modelProviderServiceConfigProviderFieldMaskMetadata `fieldmask_oneof:"Provider"`
}

type isModelProviderServiceConfig_Provider interface {
	isModelProviderServiceConfig_Provider()
}

// ModelProviderServiceConfig_Provider_Openai selects Openai for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_Openai struct {
	Openai ModelProviderServiceConfig_OpenAiProviderConfig `fieldmask:"openai"`
}

func (*ModelProviderServiceConfig_Provider_Openai) isModelProviderServiceConfig_Provider() {}

// ModelProviderServiceConfig_Provider_AzureOpenai selects AzureOpenai for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_AzureOpenai struct {
	AzureOpenai ModelProviderServiceConfig_AzureOpenAiProviderConfig `fieldmask:"azure_openai"`
}

func (*ModelProviderServiceConfig_Provider_AzureOpenai) isModelProviderServiceConfig_Provider() {}

// ModelProviderServiceConfig_Provider_Anthropic selects Anthropic for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_Anthropic struct {
	Anthropic ModelProviderServiceConfig_AnthropicProviderConfig `fieldmask:"anthropic"`
}

func (*ModelProviderServiceConfig_Provider_Anthropic) isModelProviderServiceConfig_Provider() {}

// ModelProviderServiceConfig_Provider_AmazonBedrock selects AmazonBedrock for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_AmazonBedrock struct {
	AmazonBedrock ModelProviderServiceConfig_AmazonBedrockProviderConfig `fieldmask:"amazon_bedrock"`
}

func (*ModelProviderServiceConfig_Provider_AmazonBedrock) isModelProviderServiceConfig_Provider() {}

// ModelProviderServiceConfig_Provider_Custom selects Custom for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_Custom struct {
	Custom ModelProviderServiceConfig_CustomProviderConfig `fieldmask:"custom"`
}

func (*ModelProviderServiceConfig_Provider_Custom) isModelProviderServiceConfig_Provider() {}

// ModelProviderServiceConfig_Provider_MicrosoftFoundry selects MicrosoftFoundry for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_MicrosoftFoundry struct {
	MicrosoftFoundry ModelProviderServiceConfig_MicrosoftFoundryProviderConfig `fieldmask:"microsoft_foundry"`
}

func (*ModelProviderServiceConfig_Provider_MicrosoftFoundry) isModelProviderServiceConfig_Provider() {
}

// ModelProviderServiceConfig_Provider_GeminiEnterprise selects GeminiEnterprise for ModelProviderServiceConfig.Provider.
type ModelProviderServiceConfig_Provider_GeminiEnterprise struct {
	GeminiEnterprise ModelProviderServiceConfig_GeminiEnterpriseProviderConfig `fieldmask:"gemini_enterprise"`
}

func (*ModelProviderServiceConfig_Provider_GeminiEnterprise) isModelProviderServiceConfig_Provider() {
}

type modelProviderServiceConfigProviderFieldMaskMetadata struct {
	*ModelProviderServiceConfig_Provider_Openai
	*ModelProviderServiceConfig_Provider_AzureOpenai
	*ModelProviderServiceConfig_Provider_Anthropic
	*ModelProviderServiceConfig_Provider_AmazonBedrock
	*ModelProviderServiceConfig_Provider_Custom
	*ModelProviderServiceConfig_Provider_MicrosoftFoundry
	*ModelProviderServiceConfig_Provider_GeminiEnterprise
}

// Amazon Bedrock provider configuration..
type ModelProviderServiceConfig_AmazonBedrockProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set. (-- Wrapped in a
	// oneof so future non-direct modes can be added as additional variants without
	// a breaking change. --)
	ProviderMode isModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_AmazonBedrockProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_AmazonBedrockProviderConfig.ProviderMode.
// Amazon Bedrock region and authentication configuration.
type ModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_AmazonBedrockProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_AmazonBedrockProviderConfig_ProviderMode_Direct
}

// Direct form of Amazon Bedrock provider config.
//
// Authentication is one of two mutually exclusive modes, exactly one of which
// must be supplied on Create: - Access keys: set `aws_access_key`, leave
// `service_credential` unset. - Unity Catalog service credential: set
// `service_credential.name` to the resource name `credentials/{name}`, leave
// `aws_access_key` unset. The credential value lives in Unity Catalog and is
// referenced by name, not held on this message. Setting more than one mode is
// rejected..
type ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig struct {
	// AWS region where the Bedrock endpoint is hosted (e.g., `us-east-1`). Required
	// on Create.
	Region *string `fieldmask:"region"`
	// Authentication mode. Exactly one variant may be set.
	AuthMode isModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode
	_        [0]modelProviderServiceConfig_AmazonBedrockProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_ServiceCredential selects ServiceCredential for ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig.AuthMode.
// Reference to a Unity Catalog service credential authorizing Bedrock requests.
// On Create, supply `service_credential.name` in the form `credentials/{name}`.
// Required on Create when using service-credential authentication; mutually
// exclusive with `aws_access_key`. The credential is referenced by name; its
// value is not carried here. Only supported on AWS-hosted workspaces.
type ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_ServiceCredential struct {
	ServiceCredential ModelProviderServiceConfig_ServiceCredential `fieldmask:"service_credential"`
}

func (*ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_ServiceCredential) isModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode() {
}

// ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_AwsAccessKey selects AwsAccessKey for ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig.AuthMode.
// AWS access-key-pair authentication. Set `access_key_id` and
// `secret_access_key.plaintext`. Mutually exclusive with `service_credential`.
type ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_AwsAccessKey struct {
	AwsAccessKey ModelProviderServiceConfig_AwsAccessKey `fieldmask:"aws_access_key"`
}

func (*ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_AwsAccessKey) isModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_AmazonBedrockProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_ServiceCredential
	*ModelProviderServiceConfig_AmazonBedrockProviderDirectConfig_AuthMode_AwsAccessKey
}

// Anthropic provider configuration. Exactly one of `direct` or `relayed` must
// be set on Create; the two are mutually exclusive..
type ModelProviderServiceConfig_AnthropicProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set: an inline
	// credential (`direct`) or credential-less relaying (`relayed`). (-- Anthropic
	// is the only provider with a non-direct mode today. The oneof enforces
	// direct-vs-relayed exclusivity on the wire; the validator still enforces that
	// one of them is set on Create and per-mode completeness (e.g.
	// relayed.plan_type). --)
	ProviderMode isModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_AnthropicProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_AnthropicProviderConfig.ProviderMode.
// Direct authentication with an API key supplied in `direct.api_key.plaintext`.
// Required unless `relayed` is set.
type ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_AnthropicProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode() {
}

// ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Relayed selects Relayed for ModelProviderServiceConfig_AnthropicProviderConfig.ProviderMode.
// Relayed authentication. Each inference request supplies the caller's OAuth
// token, which is forwarded to Anthropic. No Anthropic credential is stored.
// Mutually exclusive with `direct`.
type ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Relayed struct {
	Relayed ModelProviderServiceConfig_AnthropicProviderRelayedConfig `fieldmask:"relayed"`
}

func (*ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Relayed) isModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_AnthropicProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Direct
	*ModelProviderServiceConfig_AnthropicProviderConfig_ProviderMode_Relayed
}

// Direct form of Anthropic provider config..
type ModelProviderServiceConfig_AnthropicProviderDirectConfig struct {
	// Authentication mode. Exactly one variant may be set. (-- Wrapped in a oneof
	// so future auth modes (e.g. a UC service credential) can be added as
	// additional variants without a breaking change. --)
	AuthMode isModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode
	_        [0]modelProviderServiceConfig_AnthropicProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode_ApiKey selects ApiKey for ModelProviderServiceConfig_AnthropicProviderDirectConfig.AuthMode.
// Anthropic API key. Required when creating the service. Supply the value in
// `api_key.plaintext`.
type ModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode_ApiKey struct {
	ApiKey ModelProviderServiceConfig_ProviderSecret `fieldmask:"api_key"`
}

func (*ModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode_ApiKey) isModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_AnthropicProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_AnthropicProviderDirectConfig_AuthMode_ApiKey
}

// Relayed Anthropic provider configuration. Each inference request supplies the
// caller's OAuth token, which is forwarded to Anthropic. No Anthropic
// credential is stored..
type ModelProviderServiceConfig_AnthropicProviderRelayedConfig struct {
}

// AWS access-key-pair auth for Amazon Bedrock: a SigV4-signing key pair..
type ModelProviderServiceConfig_AwsAccessKey struct {
	// AWS access key ID. Required on Create when using access-key auth. Treated as
	// username-equivalent (not a secret value): round-trips on reads and is
	// scrubbed from audit logs.
	AccessKeyId *string `fieldmask:"access_key_id"`
	// AWS secret access key paired with `access_key_id`. Required when creating a
	// service with access-key authentication. Supply the value in
	// `secret_access_key.plaintext`.
	SecretAccessKey *ModelProviderServiceConfig_ProviderSecret `fieldmask:"secret_access_key"`
}

// Azure OpenAI provider configuration..
type ModelProviderServiceConfig_AzureOpenAiProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set. (-- Wrapped in a
	// oneof so future non-direct modes can be added as additional variants without
	// a breaking change. --)
	ProviderMode isModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_AzureOpenAiProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_AzureOpenAiProviderConfig.ProviderMode.
// Azure OpenAI endpoint and authentication configuration.
type ModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_AzureOpenAiProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_AzureOpenAiProviderConfig_ProviderMode_Direct
}

// Direct form of Azure OpenAI provider config. Exactly one of three
// mutually-exclusive auth modes must be supplied on Create: - API key: set
// `api_key`, leave `entra_service_principal` and `service_credential` unset. -
// Entra ID (service principal): set `entra_service_principal`, leave `api_key`
// and `service_credential` unset. - Unity Catalog service credential: set
// `service_credential.name` to the resource name `credentials/{name}`, leave
// `api_key` and `entra_service_principal` unset. The credential value lives in
// Unity Catalog and is referenced by name, not held on this message. Only
// supported on Azure-hosted workspaces. Setting more than one mode is rejected..
type ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig struct {
	// Full Azure OpenAI endpoint base URL, e.g.
	// `https://myresource.openai.azure.com`. Required on Create.
	BaseUrl *string `fieldmask:"base_url"`
	// Authentication mode. Exactly one variant may be set.
	AuthMode isModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode
	_        [0]modelProviderServiceConfig_AzureOpenAiProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ApiKey selects ApiKey for ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig.AuthMode.
// Azure OpenAI API key. Supply the value in `api_key.plaintext`. Mutually
// exclusive with Entra ID and Unity Catalog service credential authentication.
type ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ApiKey struct {
	ApiKey ModelProviderServiceConfig_ProviderSecret `fieldmask:"api_key"`
}

func (*ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ApiKey) isModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode() {
}

// ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ServiceCredential selects ServiceCredential for ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig.AuthMode.
// Reference to a Unity Catalog service credential authorizing Azure OpenAI
// requests. On Create, supply `service_credential.name` in the form
// `credentials/{name}`. Required on Create when using service-credential
// authentication; mutually exclusive with `api_key` and
// `entra_service_principal`. The credential is referenced by name; its value is
// not carried here. Only supported on Azure-hosted workspaces.
type ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ServiceCredential struct {
	ServiceCredential ModelProviderServiceConfig_ServiceCredential `fieldmask:"service_credential"`
}

func (*ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ServiceCredential) isModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode() {
}

// ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_EntraServicePrincipal selects EntraServicePrincipal for ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig.AuthMode.
// Entra ID service-principal authentication. Set `tenant_id`, `client_id`, and
// `client_secret.plaintext`. Mutually exclusive with `api_key` and
// `service_credential`.
type ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_EntraServicePrincipal struct {
	EntraServicePrincipal ModelProviderServiceConfig_EntraServicePrincipal `fieldmask:"entra_service_principal"`
}

func (*ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_EntraServicePrincipal) isModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_AzureOpenAiProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ApiKey
	*ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_ServiceCredential
	*ModelProviderServiceConfig_AzureOpenAiProviderDirectConfig_AuthMode_EntraServicePrincipal
}

// Custom OpenAI-compatible provider configuration with bearer-token
// authentication..
type ModelProviderServiceConfig_CustomProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set. (-- Wrapped in a
	// oneof so future non-direct modes can be added as additional variants without
	// a breaking change. --)
	ProviderMode isModelProviderServiceConfig_CustomProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_CustomProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_CustomProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_CustomProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_CustomProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_CustomProviderConfig.ProviderMode.
// Endpoint and authentication configuration for the custom provider.
type ModelProviderServiceConfig_CustomProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_CustomProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_CustomProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_CustomProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_CustomProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_CustomProviderConfig_ProviderMode_Direct
}

// Direct form of a custom provider configuration. Set `api_key` to the bearer
// token sent in the `Authorization` header..
type ModelProviderServiceConfig_CustomProviderDirectConfig struct {
	// Endpoint URL of the OpenAI-compatible service (e.g.,
	// `https://api.example.com/v1`). Required on Create.
	BaseUrl *string `fieldmask:"base_url"`
	// Authentication configuration. (-- Mutual exclusivity is enforced by the oneof
	// on the wire. --)
	AuthMode isModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode
	_        [0]modelProviderServiceConfig_CustomProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode_ApiKey selects ApiKey for ModelProviderServiceConfig_CustomProviderDirectConfig.AuthMode.
// Bearer token forwarded in the `Authorization` header. Supply the value in
// `api_key.plaintext`.
type ModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode_ApiKey struct {
	ApiKey ModelProviderServiceConfig_ProviderSecret `fieldmask:"api_key"`
}

func (*ModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode_ApiKey) isModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_CustomProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_CustomProviderDirectConfig_AuthMode_ApiKey
}

// Entra ID (Azure AD) service-principal authentication. The `tenant_id` and
// `client_id` identify the service principal, and `client_secret` authenticates
// it. AI Gateway exchanges these credentials for an Entra bearer token for
// requests to Azure OpenAI or Microsoft Foundry..
type ModelProviderServiceConfig_EntraServicePrincipal struct {
	// Entra ID (Azure AD) tenant ID. Required on Create.
	TenantId *string `fieldmask:"tenant_id"`
	// Entra ID client (application) ID. Required on Create.
	ClientId *string `fieldmask:"client_id"`
	// How the service principal proves its identity. Exactly one variant must be
	// set on Create. Today only `client_secret` is supported. (-- A oneof so
	// additional proof mechanisms can be added as non-breaking variants without
	// changing the tenant_id / client_id identity fields. --)
	Credential isModelProviderServiceConfig_EntraServicePrincipal_Credential
	_          [0]modelProviderServiceConfig_EntraServicePrincipalCredentialFieldMaskMetadata `fieldmask_oneof:"Credential"`
}

type isModelProviderServiceConfig_EntraServicePrincipal_Credential interface {
	isModelProviderServiceConfig_EntraServicePrincipal_Credential()
}

// ModelProviderServiceConfig_EntraServicePrincipal_Credential_ClientSecret selects ClientSecret for ModelProviderServiceConfig_EntraServicePrincipal.Credential.
// Entra ID client secret. Supply the value in `client_secret.plaintext`.
type ModelProviderServiceConfig_EntraServicePrincipal_Credential_ClientSecret struct {
	ClientSecret ModelProviderServiceConfig_ProviderSecret `fieldmask:"client_secret"`
}

func (*ModelProviderServiceConfig_EntraServicePrincipal_Credential_ClientSecret) isModelProviderServiceConfig_EntraServicePrincipal_Credential() {
}

type modelProviderServiceConfig_EntraServicePrincipalCredentialFieldMaskMetadata struct {
	*ModelProviderServiceConfig_EntraServicePrincipal_Credential_ClientSecret
}

// Gemini Enterprise provider configuration..
type ModelProviderServiceConfig_GeminiEnterpriseProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set. (-- Wrapped in a
	// oneof so future non-direct modes can be added as additional variants without
	// a breaking change. --)
	ProviderMode isModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_GeminiEnterpriseProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_GeminiEnterpriseProviderConfig.ProviderMode.
// Gemini Enterprise project, region, and authentication configuration.
type ModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_GeminiEnterpriseProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_GeminiEnterpriseProviderConfig_ProviderMode_Direct
}

// Direct Gemini Enterprise provider configuration. An API key is required when
// creating the service..
type ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig struct {
	// Authentication mode. Exactly one variant may be set.
	AuthMode isModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode
	// GCP project ID hosting the Gemini Enterprise endpoint. Required on Create.
	ProjectId *string `fieldmask:"project_id"`
	// GCP region of the Gemini Enterprise endpoint (e.g., `us-central1`). Required
	// on Create.
	Region *string                                                                                     `fieldmask:"region"`
	_      [0]modelProviderServiceConfig_GeminiEnterpriseProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode_ApiKey selects ApiKey for ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig.AuthMode.
// Google Gemini Enterprise API key. Required when creating the service. Supply
// the value in `api_key.plaintext`.
type ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode_ApiKey struct {
	ApiKey ModelProviderServiceConfig_ProviderSecret `fieldmask:"api_key"`
}

func (*ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode_ApiKey) isModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_GeminiEnterpriseProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_GeminiEnterpriseProviderDirectConfig_AuthMode_ApiKey
}

// Microsoft Foundry provider configuration..
type ModelProviderServiceConfig_MicrosoftFoundryProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set. (-- Wrapped in a
	// oneof so future non-direct modes can be added as additional variants without
	// a breaking change. --)
	ProviderMode isModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_MicrosoftFoundryProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_MicrosoftFoundryProviderConfig.ProviderMode.
// Microsoft Foundry endpoint and authentication configuration.
type ModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_MicrosoftFoundryProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_MicrosoftFoundryProviderConfig_ProviderMode_Direct
}

// Direct form of Microsoft Foundry provider config.
//
// Authentication is one of three mutually exclusive modes, exactly one of which
// must be supplied on Create: - API key: set `api_key`, leave
// `entra_service_principal` and `service_credential` unset. - Entra ID (service
// principal): set `entra_service_principal`, leave `api_key` and
// `service_credential` unset. AI Gateway exchanges these for an Entra bearer
// token on outbound requests via the OAuth2 client-credentials grant. - Unity
// Catalog service credential: set `service_credential.name` to the resource
// name `credentials/{name}`, leave `api_key` and `entra_service_principal`
// unset. The credential value lives in Unity Catalog and is referenced by name,
// not held on this message. Only supported on Azure-hosted workspaces. Setting
// more than one mode is rejected..
type ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig struct {
	// Microsoft Foundry endpoint URL. Required on Create.
	BaseUrl *string `fieldmask:"base_url"`
	// Authentication mode. Exactly one variant may be set.
	AuthMode isModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode
	_        [0]modelProviderServiceConfig_MicrosoftFoundryProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ApiKey selects ApiKey for ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig.AuthMode.
// Microsoft Foundry API key. Supply the value in `api_key.plaintext`. Mutually
// exclusive with Entra ID and Unity Catalog service credential authentication.
type ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ApiKey struct {
	ApiKey ModelProviderServiceConfig_ProviderSecret `fieldmask:"api_key"`
}

func (*ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ApiKey) isModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode() {
}

// ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ServiceCredential selects ServiceCredential for ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig.AuthMode.
// Reference to a Unity Catalog service credential authorizing Microsoft Foundry
// requests. On Create, supply `service_credential.name` in the form
// `credentials/{name}`. Required on Create when using service-credential
// authentication; mutually exclusive with `api_key` and
// `entra_service_principal`. The credential is referenced by name; its value is
// not carried here. Only supported on Azure-hosted workspaces.
type ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ServiceCredential struct {
	ServiceCredential ModelProviderServiceConfig_ServiceCredential `fieldmask:"service_credential"`
}

func (*ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ServiceCredential) isModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode() {
}

// ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_EntraServicePrincipal selects EntraServicePrincipal for ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig.AuthMode.
// Entra ID service-principal authentication. Set `tenant_id`, `client_id`, and
// `client_secret.plaintext`. Mutually exclusive with `api_key` and
// `service_credential`.
type ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_EntraServicePrincipal struct {
	EntraServicePrincipal ModelProviderServiceConfig_EntraServicePrincipal `fieldmask:"entra_service_principal"`
}

func (*ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_EntraServicePrincipal) isModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_MicrosoftFoundryProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ApiKey
	*ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_ServiceCredential
	*ModelProviderServiceConfig_MicrosoftFoundryProviderDirectConfig_AuthMode_EntraServicePrincipal
}

// Model target configuration for an external model destination..
type ModelProviderServiceConfig_ModelTargetConfig struct {
	// Provider-side model identifier, such as `gpt-5` or `claude-opus-4-7`. This
	// identifies a model at the upstream provider; it is not a Unity Catalog model
	// resource.
	Model *string
	// Provider-native API types supported by this model, such as
	// `openai/v1/chat/completions`. At least one value is required. AI Gateway uses
	// these values to translate requests and responses. At most 64 entries of 256
	// characters each are allowed.
	NativeApiTypes []string
}

// OpenAI provider configuration..
type ModelProviderServiceConfig_OpenAiProviderConfig struct {
	// Provider configuration mode. Exactly one variant may be set. (-- Wrapped in a
	// oneof so future non-direct modes can be added as additional variants without
	// a breaking change. --)
	ProviderMode isModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode
	_            [0]modelProviderServiceConfig_OpenAiProviderConfigProviderModeFieldMaskMetadata `fieldmask_oneof:"ProviderMode"`
}

type isModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode interface {
	isModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode()
}

// ModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode_Direct selects Direct for ModelProviderServiceConfig_OpenAiProviderConfig.ProviderMode.
// OpenAI configuration with an API key supplied in the request.
type ModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode_Direct struct {
	Direct ModelProviderServiceConfig_OpenAiProviderDirectConfig `fieldmask:"direct"`
}

func (*ModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode_Direct) isModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode() {
}

type modelProviderServiceConfig_OpenAiProviderConfigProviderModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_OpenAiProviderConfig_ProviderMode_Direct
}

// Direct (inline-credentials) form of the OpenAI provider config..
type ModelProviderServiceConfig_OpenAiProviderDirectConfig struct {
	// Authentication mode. Exactly one variant may be set. (-- Wrapped in a oneof
	// so future auth modes (e.g. a UC service credential) can be added as
	// additional variants without a breaking change. --)
	AuthMode isModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode
	// Optional OpenAI organization ID. When set, the platform forwards it as the
	// `OpenAI-Organization` header.
	Organization *string `fieldmask:"organization"`
	// Optional custom base URL. Defaults to `https://api.openai.com/v1`. Use for
	// OpenAI-API-compatible third-party endpoints or in-network proxies.
	BaseUrl *string                                                                           `fieldmask:"base_url"`
	_       [0]modelProviderServiceConfig_OpenAiProviderDirectConfigAuthModeFieldMaskMetadata `fieldmask_oneof:"AuthMode"`
}

type isModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode interface {
	isModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode()
}

// ModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode_ApiKey selects ApiKey for ModelProviderServiceConfig_OpenAiProviderDirectConfig.AuthMode.
// OpenAI API key. Required when creating the service. Supply the value in
// `api_key.plaintext`.
type ModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode_ApiKey struct {
	ApiKey ModelProviderServiceConfig_ProviderSecret `fieldmask:"api_key"`
}

func (*ModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode_ApiKey) isModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode() {
}

type modelProviderServiceConfig_OpenAiProviderDirectConfigAuthModeFieldMaskMetadata struct {
	*ModelProviderServiceConfig_OpenAiProviderDirectConfig_AuthMode_ApiKey
}

// A secret value supplied as part of an inline provider config. The caller
// supplies the value as inline `plaintext` on writes; the platform stores it
// encrypted. The `plaintext` field is `INPUT_ONLY` and never round-trips on
// reads..
type ModelProviderServiceConfig_ProviderSecret struct {
	// How the credential value is supplied. Exactly one variant may be set. (--
	// Wrapped in a oneof so a non-plaintext source can be added as an additional
	// variant without a breaking change; `secret_reference` is that variant, and
	// further sources can follow the same way. --)
	Value isModelProviderServiceConfig_ProviderSecret_Value
	_     [0]modelProviderServiceConfig_ProviderSecretValueFieldMaskMetadata `fieldmask_oneof:"Value"`
}

type isModelProviderServiceConfig_ProviderSecret_Value interface {
	isModelProviderServiceConfig_ProviderSecret_Value()
}

// ModelProviderServiceConfig_ProviderSecret_Value_Plaintext selects Plaintext for ModelProviderServiceConfig_ProviderSecret.Value.
// Inline plaintext credential. INPUT_ONLY: the value never round-trips on
// reads. Get and List responses omit `plaintext`; the enclosing secret object
// remains present to indicate that a secret is configured.
type ModelProviderServiceConfig_ProviderSecret_Value_Plaintext struct {
	Plaintext string `fieldmask:"plaintext"`
}

func (*ModelProviderServiceConfig_ProviderSecret_Value_Plaintext) isModelProviderServiceConfig_ProviderSecret_Value() {
}

type modelProviderServiceConfig_ProviderSecretValueFieldMaskMetadata struct {
	*ModelProviderServiceConfig_ProviderSecret_Value_Plaintext
}

// The customer-owned Unity Catalog service credential a model provider service
// uses to authenticate to its provider, referenced by name..
type ModelProviderServiceConfig_ServiceCredential struct {
	// Resource name of the bound Unity Catalog service credential, in the form
	// `credentials/{name}`. Supply this field when creating the service or
	// rebinding its credential. On read, it reflects the credential's current name.
	Name *string `fieldmask:"name"`
}

// A governed AI Gateway endpoint in Unity Catalog that routes inference
// requests to one or more destinations, such as a <Databricks> foundation model
// or an external model reached through a model provider service. Applies
// centralized access control, rate limits, and auditing to its traffic..
type ModelService struct {
	// Resource name of the model service. Format:
	// `model-services/{catalog}.{schema}.{model_service}`. Each `{...}` component
	// is capped at 255 characters individually. Server-derived on Create from
	// `parent` + `model_service_id`; required and immutable on Update/Get/Delete.
	Name *string `fieldmask:"name"`
	// Owner of the model service.
	EffectiveOwner *string `fieldmask:"effective_owner"`
	// Metastore hosting the model service.
	MetastoreId *string `fieldmask:"metastore_id"`
	// Time the model service was created.
	CreateTime *types.Time `fieldmask:"create_time"`
	// Creator identity.
	CreatedBy *string `fieldmask:"created_by"`
	// Time the model service was last modified.
	UpdateTime *types.Time `fieldmask:"update_time"`
	// Identity of the last updater.
	UpdatedBy *string `fieldmask:"updated_by"`
	// User-provided description.
	Comment *string `fieldmask:"comment"`
	// Destinations, routing, rate limits, and payload logging configuration.
	// Required on Create. On Update, provide this field when `update_mask` contains
	// `config` or one of its subpaths.
	Config *ModelServiceConfig `fieldmask:"config"`
	// Optimistic concurrency token returned on every read. To make an Update or
	// Delete conditional, pass the last-read value in that request's `etag` field.
	// In REST responses, this value is a base64 string; URL-encode it when setting
	// the `etag` query parameter.
	Etag []byte `fieldmask:"etag"`
	// API types supported across this service's destinations, such as
	// `openai/v1/chat/completions`, `openai/v1/embeddings`, and
	// `mlflow/v1/chat/completions`. Derived from the backing models and providers
	// at read time.
	SupportedApiTypes []string `fieldmask:"supported_api_types"`
}

// Operational configuration wrapped around the ModelService resource..
type ModelServiceConfig struct {
	// Routing configuration: destinations and fallback.
	Routing *ModelServiceConfig_RoutingConfig `fieldmask:"routing"`
	// Rate limits applied to requests routed through this model service.
	RateLimits []RateLimit `fieldmask:"rate_limits"`
	// Inference table configuration for payload logging.
	InferenceTable *InferenceTableConfig `fieldmask:"inference_table"`
}

// A destination the model service can route traffic to. Exactly one of the
// per-type configs inside `type_config` must be set, and it must match
// `destination_type`..
type ModelServiceConfig_DestinationConfig struct {
	// User-facing label for this destination, used in routing references.
	Name *string
	// Backing-model category. Provide the matching type-specific configuration and
	// leave the other type-specific configurations unset.
	DestinationType ModelServiceConfig_DestinationConfig_DestinationType
	// Percentage of primary traffic sent to this destination, from 0 to 100.
	// Required when there is more than one primary destination, in which case the
	// primary percentages must sum to 100; a single primary destination receives
	// all traffic. Fallback destinations are ordered and do not use this field.
	TrafficPercentage *int
	// Destination-type-specific configuration.
	TypeConfig isModelServiceConfig_DestinationConfig_TypeConfig
	// Whether the destination's backing model or model provider service has been
	// deleted. The destination remains visible so you can identify the broken
	// dependency. Requests cannot use this destination until the backing resource
	// is restored or the destination is replaced.
	IsDeleted *bool
}

type isModelServiceConfig_DestinationConfig_TypeConfig interface {
	isModelServiceConfig_DestinationConfig_TypeConfig()
}

// ModelServiceConfig_DestinationConfig_TypeConfig_PayPerTokenConfig selects PayPerTokenConfig for ModelServiceConfig_DestinationConfig.TypeConfig.
// Configuration for a pay-per-token <Databricks> foundation model.
type ModelServiceConfig_DestinationConfig_TypeConfig_PayPerTokenConfig struct {
	PayPerTokenConfig ModelServiceConfig_PayPerTokenConfig
}

func (*ModelServiceConfig_DestinationConfig_TypeConfig_PayPerTokenConfig) isModelServiceConfig_DestinationConfig_TypeConfig() {
}

// ModelServiceConfig_DestinationConfig_TypeConfig_ProvisionedThroughputConfig selects ProvisionedThroughputConfig for ModelServiceConfig_DestinationConfig.TypeConfig.
// Configuration for a provisioned-throughput <Databricks> foundation model.
type ModelServiceConfig_DestinationConfig_TypeConfig_ProvisionedThroughputConfig struct {
	ProvisionedThroughputConfig ModelServiceConfig_ProvisionedThroughputConfig
}

func (*ModelServiceConfig_DestinationConfig_TypeConfig_ProvisionedThroughputConfig) isModelServiceConfig_DestinationConfig_TypeConfig() {
}

// ModelServiceConfig_DestinationConfig_TypeConfig_ExternalModelConfig selects ExternalModelConfig for ModelServiceConfig_DestinationConfig.TypeConfig.
// Configuration for an external model reached through a model provider service.
type ModelServiceConfig_DestinationConfig_TypeConfig_ExternalModelConfig struct {
	ExternalModelConfig ModelServiceConfig_ExternalModelConfig
}

func (*ModelServiceConfig_DestinationConfig_TypeConfig_ExternalModelConfig) isModelServiceConfig_DestinationConfig_TypeConfig() {
}

// Configuration for an external-foundation-model destination. Provider auth and
// provider-specific cloud configuration are owned by a separate, governed
// ModelProviderService entity referenced via `model_provider_service`; the
// platform resolves the provider at invocation time..
type ModelServiceConfig_ExternalModelConfig struct {
	// Resource name of the governed ModelProviderService that owns provider auth
	// and provider-specific configuration. The referenced ModelProviderService also
	// carries the provider type, so this message does not surface it directly.
	// Format:
	// `model-provider-services/{catalog}.{schema}.{model_provider_service}`. Each
	// `{...}` component is capped at 255 characters individually.
	ModelProviderService *string
	// Routing target for the destination: the provider-side model selected from the
	// referenced ModelProviderService's `targets` catalog, plus the unified API
	// types the platform should translate to/from at request time.
	Target *ModelProviderServiceConfig_ModelTargetConfig
}

// Fallback routing applied after a primary destination fails. Fallback
// destinations are tried in the listed order..
type ModelServiceConfig_FallbackConfig struct {
	// Fallback destinations, tried in the listed order. At most 5 are allowed.
	Destinations []ModelServiceConfig_DestinationConfig `fieldmask:"destinations"`
}

// Configuration for a pay-per-token foundation-model destination. Identifies
// the foundation model by its UC resource name; the platform resolves it to a
// Model Serving endpoint at request time..
type ModelServiceConfig_PayPerTokenConfig struct {
	// Resource name of the Unity Catalog model. Format:
	// `models/{catalog}.{schema}.{model}`.
	Model *string
}

// Configuration for a provisioned-throughput foundation-model destination.
// References a pre-existing Model Serving endpoint that serves the model;
// sizing (provisioned throughput, burst scaling, model version) is owned by the
// Model Serving endpoint itself, not by this message..
type ModelServiceConfig_ProvisionedThroughputConfig struct {
	// Name of the backing Model Serving endpoint serving the provisioned-
	// throughput foundation model, in the form `serving-endpoints/{name}`. The same
	// Unity Catalog model can be served on multiple Model Serving endpoints with
	// different throughput, regions, or configurations. The caller selects the
	// endpoint to which this destination routes. The endpoint must exist at create
	// time.
	ModelServingEndpoint *string
	// UC model FQN of the model served by the backing endpoint (e.g.,
	// `system.ai.databricks-claude-opus-4-6`). Resolved from Model Serving at
	// Create/Update time.
	Model *string
}

// Routing configuration for a model service, nesting destinations and fallback
// under a single sub-message..
type ModelServiceConfig_RoutingConfig struct {
	// Primary routing destinations. At most 10 are allowed. At least one is
	// required on Create. On Update, provide this list when replacing the full
	// `config` or updating `config.routing.destinations`; other granular routing
	// updates do not require resending destinations. The intermediate
	// `config.routing` mask path is not supported.
	Destinations []ModelServiceConfig_DestinationConfig `fieldmask:"destinations"`
	// Fallback routing applied after a primary destination fails. Fallback
	// destinations are tried in the listed order.
	Fallback *ModelServiceConfig_FallbackConfig `fieldmask:"fallback"`
}

// A rate limit applied to service requests. Leave `requests` or `tokens` unset
// to impose no limit on that dimension; set a value to cap that dimension
// within the renewal period..
type RateLimit struct {
	// Scope of the rate limit. Depending on this value, the limit applies to a
	// principal, the service as a whole, or each user by default.
	Key RateLimit_RateLimitKey
	// Renewal period.
	RenewalPeriod RateLimit_RateLimitRenewalPeriod
	// Principal this limit applies to: user email, group name, or service principal
	// application ID. Required when `key` applies to a user, group, or service
	// principal; otherwise it must be unset.
	Principal *string
	// Maximum requests allowed in one renewal period. Leave unset for no request
	// limit. Set to `0` to deny all requests.
	Requests *int64
	// Maximum tokens allowed in one renewal period. Leave unset for no token limit.
	// Set to `0` to deny all requests.
	Tokens *int64
}

// Request to update an MCP service. `name` cannot appear in `update_mask`..
type UpdateMcpServiceRequest struct {
	// The MCP service with the updated field values. `name` identifies the resource
	// (`mcp-services/{catalog}.{schema}.{mcp_service}`); only fields listed in
	// `update_mask` are applied.
	McpService *McpService
	// Fields to update. Use `config` to replace the entire configuration. The
	// replacement must include every required field; any optional field you omit is
	// cleared. To preserve sibling fields, use one or more granular paths:
	// `comment`, `config.source_connection.name`, `config.include_tool_selectors`,
	// or `config.rate_limits`. Wildcard paths such as `*` are not supported.
	UpdateMask *types.FieldMask[McpService]
	// Optimistic concurrency token from the most recent read. When set, the update
	// succeeds only if the resource has not changed. Leave unset for an
	// unconditional update. For REST requests, URL-encode the base64 string
	// returned by the API when setting the `etag` query parameter.
	Etag []byte
}

// Request to update a model provider service. `name` and `config.provider_type`
// cannot appear in `update_mask`..
type UpdateModelProviderServiceRequest struct {
	// The model provider service with the updated field values. `name` identifies
	// the resource
	// (`model-provider-services/{catalog}.{schema}.{model_provider_service}`); only
	// fields listed in `update_mask` are applied.
	ModelProviderService *ModelProviderService
	// Fields to update. Use `config` to replace the entire configuration. The
	// replacement must include every required field; any optional field you omit is
	// cleared. To preserve sibling fields, use one or more granular paths:
	// `comment`; `config.provider` to replace the active provider-specific value
	// (for example, `config.openai`; the mask path remains `config.provider`);
	// `config.allow_all_targets`, `config.targets`, `config.forward_headers`,
	// `config.forward_query_parameters`, `config.forward_unmanaged_paths`,
	// `config.rate_limits`, or `config.inference_table`. The provider type is
	// immutable, and wildcard paths such as `*` are not supported.
	UpdateMask *types.FieldMask[ModelProviderService]
	// Optimistic concurrency token from the most recent read. When set, the update
	// succeeds only if the resource has not changed. Leave unset for an
	// unconditional update. For REST requests, URL-encode the base64 string
	// returned by the API when setting the `etag` query parameter.
	Etag []byte
}

// Request to update a model service. `name` cannot appear in `update_mask`; the
// model service name is immutable..
type UpdateModelServiceRequest struct {
	// The model service with the updated field values. `name` identifies the
	// resource (`model-services/{catalog}.{schema}.{model_service}`); only fields
	// listed in `update_mask` are applied.
	ModelService *ModelService
	// Fields to update. Use `config` to replace the entire configuration. The
	// replacement must include every required field; any optional field you omit is
	// cleared. To preserve sibling fields, use one or more granular paths:
	// `comment`, `config.routing.destinations`,
	// `config.routing.fallback.destinations`, `config.rate_limits`, or
	// `config.inference_table`. Intermediate paths such as `config.routing` and
	// `config.routing.fallback`, and wildcard paths such as `*`, are not supported.
	UpdateMask *types.FieldMask[ModelService]
	// Optimistic concurrency token from the most recent read. When set, the update
	// succeeds only if the resource has not changed. Leave unset for an
	// unconditional update. For REST requests, URL-encode the base64 string
	// returned by the API when setting the `etag` query parameter.
	Etag []byte
}
