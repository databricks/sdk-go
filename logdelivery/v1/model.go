// Code generated by Databricks SDK Generator. DO NOT EDIT.

package logdelivery

// * Log Delivery Status
//
// `ENABLED`: All dependencies have executed and succeeded `DISABLED`: At least
// one dependency has succeeded
type LogDeliveryConfigStatus string

const (
	LogDeliveryConfigStatus_Unspecified LogDeliveryConfigStatus = ""
	// Configuration is enabled
	LogDeliveryConfigStatus_Enabled LogDeliveryConfigStatus = "ENABLED"
	// Configuration is disabled
	LogDeliveryConfigStatus_Disabled LogDeliveryConfigStatus = "DISABLED"
)

// * Log Delivery Output Format
type LogDeliveryOutputFormat string

const (
	LogDeliveryOutputFormat_Unspecified LogDeliveryOutputFormat = ""
	// Deliver CSV files
	LogDeliveryOutputFormat_Csv LogDeliveryOutputFormat = "CSV"
	// Deliver JSON files
	LogDeliveryOutputFormat_Json LogDeliveryOutputFormat = "JSON"
)

// * The status string for log delivery. Possible values are: `CREATED`: There
// were no log delivery attempts since the config was created. `SUCCEEDED`: The
// latest attempt of log delivery has succeeded completely. `USER_FAILURE`: The
// latest attempt of log delivery failed because of misconfiguration of customer
// provided permissions on role or storage. `SYSTEM_FAILURE`: The latest attempt
// of log delivery failed because of an <Databricks> internal error. Contact
// support if it doesn't go away soon. `NOT_FOUND`: The log delivery status as
// the configuration has been disabled since the release of this feature or
// there are no workspaces in the account.
type LogDeliveryStatusEnum string

const (
	LogDeliveryStatusEnum_Unspecified LogDeliveryStatusEnum = ""
	// Configuration is just created and logs haven't delivered yet
	LogDeliveryStatusEnum_Created LogDeliveryStatusEnum = "CREATED"
	// Configuration has succeeded in the last run
	LogDeliveryStatusEnum_Succeeded LogDeliveryStatusEnum = "SUCCEEDED"
	// Configuration has failed in the last run due to user failure
	LogDeliveryStatusEnum_UserFailure LogDeliveryStatusEnum = "USER_FAILURE"
	// Configuration has failed in the last run due to system failure
	LogDeliveryStatusEnum_SystemFailure LogDeliveryStatusEnum = "SYSTEM_FAILURE"
	// Status not found
	LogDeliveryStatusEnum_NotFound LogDeliveryStatusEnum = "NOT_FOUND"
)

// * Log Delivery Type
type LogDeliveryType string

const (
	LogDeliveryType_Unspecified LogDeliveryType = ""
	// Deliver Billable Usage logs
	LogDeliveryType_BillableUsage LogDeliveryType = "BILLABLE_USAGE"
	// Deliver Audit Logs
	LogDeliveryType_AuditLogs LogDeliveryType = "AUDIT_LOGS"
)

// * Log Delivery Configuration.
type CreateLogDeliveryConfigurationParams struct {
	// The unique UUID of log delivery configuration
	ConfigId *string
	// The optional human-readable name of the log delivery configuration. Defaults
	// to empty.
	ConfigName *string
	// Log delivery type. Supported values are: * `BILLABLE_USAGE` — Configure
	// [billable usage log delivery]. For the CSV schema, see the [View billable
	// usage]. * `AUDIT_LOGS` — Configure [audit log delivery]. For the JSON
	// schema, see [Configure audit logging]
	//
	// [Configure audit logging]: https://docs.databricks.com/administration-guide/account-settings/audit-logs.html
	// [View billable usage]: https://docs.databricks.com/administration-guide/account-settings/usage.html
	// [audit log delivery]: https://docs.databricks.com/administration-guide/account-settings/audit-logs.html
	// [billable usage log delivery]: https://docs.databricks.com/administration-guide/account-settings/billable-usage-delivery.html
	LogType LogDeliveryType
	// The file type of log delivery. * If `log_type` is `BILLABLE_USAGE`, this
	// value must be `CSV`. Only the CSV (comma-separated values) format is
	// supported. For the schema, see the [View billable usage] * If `log_type` is
	// `AUDIT_LOGS`, this value must be `JSON`. Only the JSON (JavaScript Object
	// Notation) format is supported. For the schema, see the [Configuring audit
	// logs].
	//
	// [Configuring audit logs]: https://docs.databricks.com/administration-guide/account-settings/audit-logs.html
	// [View billable usage]: https://docs.databricks.com/administration-guide/account-settings/usage.html
	OutputFormat LogDeliveryOutputFormat
	// <Databricks> account ID.
	AccountId *string
	// The ID for a method:credentials/create that represents the AWS IAM role with
	// policy and trust relationship as described in the main billable usage
	// documentation page. See [Configure billable usage delivery].
	//
	// [Configure billable usage delivery]: https://docs.databricks.com/administration-guide/account-settings/billable-usage-delivery.html
	CredentialsId *string
	// The ID for a method:storage/create that represents the S3 bucket with bucket
	// policy as described in the main billable usage documentation page. See
	// [Configure billable usage delivery].
	//
	// [Configure billable usage delivery]: https://docs.databricks.com/administration-guide/account-settings/billable-usage-delivery.html
	StorageConfigurationId *string
	// Optional filter that specifies workspace IDs to deliver logs for. By default
	// the workspace filter is empty and log delivery applies at the account level,
	// delivering workspace-level logs for all workspaces in your account, plus
	// account level logs. You can optionally set this field to an array of
	// workspace IDs (each one is an `int64`) to which log delivery should apply, in
	// which case only workspace-level logs relating to the specified workspaces are
	// delivered. If you plan to use different log delivery configurations for
	// different workspaces, set this field explicitly. Be aware that delivery
	// configurations mentioning specific workspaces won't apply to new workspaces
	// created in the future, and delivery won't include account level logs. For
	// some types of <Databricks> deployments there is only one workspace per
	// account ID, so this field is unnecessary.
	WorkspaceIdsFilter []int64
	// The optional delivery path prefix within Amazon S3 storage. Defaults to
	// empty, which means that logs are delivered to the root of the bucket. This
	// must be a valid S3 object key. This must not start or end with a slash
	// character.
	DeliveryPathPrefix *string
	// This field applies only if log_type is BILLABLE_USAGE. This is the optional
	// start month and year for delivery, specified in YYYY-MM format. Defaults to
	// current year and month. BILLABLE_USAGE logs are not available for usage
	// before March 2019 (2019-03).
	DeliveryStartTime *string
	// Status of log delivery configuration. Set to `ENABLED` (enabled) or
	// `DISABLED` (disabled). Defaults to `ENABLED`. You can [enable or disable the
	// configuration](#operation/patch-log-delivery-config-status) later. Deletion
	// of a configuration is not supported, so disable a log delivery configuration
	// that is no longer needed.
	Status LogDeliveryConfigStatus
	// Time in epoch milliseconds when the log delivery configuration was created.
	CreationTime *int64
	// Time in epoch milliseconds when the log delivery configuration was updated.
	UpdateTime *int64
	// The LogDeliveryStatus of this log delivery configuration
	LogDeliveryStatus *LogDeliveryStatus
}

// * Properties of the new log delivery configuration..
type CreateLogDeliveryConfigurationRequest struct {
	LogDeliveryConfiguration *CreateLogDeliveryConfigurationParams
}

type CreateLogDeliveryConfigurationResponse struct {
	// The created log delivery configuration
	LogDeliveryConfiguration *LogDeliveryConfiguration
}

// * Get Log Delivery Configuration.
type GetLogDeliveryConfigurationRequest struct {
	// The log delivery configuration id of customer
	ConfigId *string
	// <Databricks> account ID.
	AccountId *string
}

type GetLogDeliveryConfigurationResponse struct {
	// The fetched log delivery configuration
	LogDeliveryConfiguration *LogDeliveryConfiguration
}

// * List Log Delivery Configuration.
type ListLogDeliveryConfigurationRequest struct {
	// <Databricks> account ID.
	AccountId *string
	// The Credentials id to filter the search results with
	CredentialsId *string
	// The Storage Configuration id to filter the search results with
	StorageConfigurationId *string
	// The log delivery status to filter the search results with
	Status LogDeliveryConfigStatus
	// A page token received from a previous get all budget configurations call.
	// This token can be used to retrieve the subsequent page. Requests first page
	// if absent.
	PageToken *string
}

type ListLogDeliveryConfigurationResponse struct {
	// Log delivery configurations were returned successfully.
	LogDeliveryConfigurations []LogDeliveryConfiguration
	// Token which can be sent as `page_token` to retrieve the next page of results.
	// If this field is omitted, there are no subsequent budgets.
	NextPageToken *string
}

// * Log Delivery Configuration.
type LogDeliveryConfiguration struct {
	// The unique UUID of log delivery configuration
	ConfigId *string
	// The optional human-readable name of the log delivery configuration. Defaults
	// to empty.
	ConfigName *string
	// Log delivery type. Supported values are: * `BILLABLE_USAGE` — Configure
	// [billable usage log delivery]. For the CSV schema, see the [View billable
	// usage]. * `AUDIT_LOGS` — Configure [audit log delivery]. For the JSON
	// schema, see [Configure audit logging]
	//
	// [Configure audit logging]: https://docs.databricks.com/administration-guide/account-settings/audit-logs.html
	// [View billable usage]: https://docs.databricks.com/administration-guide/account-settings/usage.html
	// [audit log delivery]: https://docs.databricks.com/administration-guide/account-settings/audit-logs.html
	// [billable usage log delivery]: https://docs.databricks.com/administration-guide/account-settings/billable-usage-delivery.html
	LogType LogDeliveryType
	// The file type of log delivery. * If `log_type` is `BILLABLE_USAGE`, this
	// value must be `CSV`. Only the CSV (comma-separated values) format is
	// supported. For the schema, see the [View billable usage] * If `log_type` is
	// `AUDIT_LOGS`, this value must be `JSON`. Only the JSON (JavaScript Object
	// Notation) format is supported. For the schema, see the [Configuring audit
	// logs].
	//
	// [Configuring audit logs]: https://docs.databricks.com/administration-guide/account-settings/audit-logs.html
	// [View billable usage]: https://docs.databricks.com/administration-guide/account-settings/usage.html
	OutputFormat LogDeliveryOutputFormat
	// <Databricks> account ID.
	AccountId *string
	// The ID for a method:credentials/create that represents the AWS IAM role with
	// policy and trust relationship as described in the main billable usage
	// documentation page. See [Configure billable usage delivery].
	//
	// [Configure billable usage delivery]: https://docs.databricks.com/administration-guide/account-settings/billable-usage-delivery.html
	CredentialsId *string
	// The ID for a method:storage/create that represents the S3 bucket with bucket
	// policy as described in the main billable usage documentation page. See
	// [Configure billable usage delivery].
	//
	// [Configure billable usage delivery]: https://docs.databricks.com/administration-guide/account-settings/billable-usage-delivery.html
	StorageConfigurationId *string
	// Optional filter that specifies workspace IDs to deliver logs for. By default
	// the workspace filter is empty and log delivery applies at the account level,
	// delivering workspace-level logs for all workspaces in your account, plus
	// account level logs. You can optionally set this field to an array of
	// workspace IDs (each one is an `int64`) to which log delivery should apply, in
	// which case only workspace-level logs relating to the specified workspaces are
	// delivered. If you plan to use different log delivery configurations for
	// different workspaces, set this field explicitly. Be aware that delivery
	// configurations mentioning specific workspaces won't apply to new workspaces
	// created in the future, and delivery won't include account level logs. For
	// some types of <Databricks> deployments there is only one workspace per
	// account ID, so this field is unnecessary.
	WorkspaceIdsFilter []int64
	// The optional delivery path prefix within Amazon S3 storage. Defaults to
	// empty, which means that logs are delivered to the root of the bucket. This
	// must be a valid S3 object key. This must not start or end with a slash
	// character.
	DeliveryPathPrefix *string
	// This field applies only if log_type is BILLABLE_USAGE. This is the optional
	// start month and year for delivery, specified in YYYY-MM format. Defaults to
	// current year and month. BILLABLE_USAGE logs are not available for usage
	// before March 2019 (2019-03).
	DeliveryStartTime *string
	// Status of log delivery configuration. Set to `ENABLED` (enabled) or
	// `DISABLED` (disabled). Defaults to `ENABLED`. You can [enable or disable the
	// configuration](#operation/patch-log-delivery-config-status) later. Deletion
	// of a configuration is not supported, so disable a log delivery configuration
	// that is no longer needed.
	Status LogDeliveryConfigStatus
	// Time in epoch milliseconds when the log delivery configuration was created.
	CreationTime *int64
	// Time in epoch milliseconds when the log delivery configuration was updated.
	UpdateTime *int64
	// The LogDeliveryStatus of this log delivery configuration
	LogDeliveryStatus *LogDeliveryStatus
}

type LogDeliveryStatus struct {
	// Enum that describes the status. Possible values are: * `CREATED`: There were
	// no log delivery attempts since the config was created. * `SUCCEEDED`: The
	// latest attempt of log delivery has succeeded completely. * `USER_FAILURE`:
	// The latest attempt of log delivery failed because of misconfiguration of
	// customer provided permissions on role or storage. * `SYSTEM_FAILURE`: The
	// latest attempt of log delivery failed because of an <Databricks> internal
	// error. Contact support if it doesn't go away soon. * `NOT_FOUND`: The log
	// delivery status as the configuration has been disabled since the release of
	// this feature or there are no workspaces in the account.
	Status LogDeliveryStatusEnum
	// The UTC time for the latest log delivery attempt.
	LastAttemptTime *string
	// The UTC time for the latest successful log delivery.
	LastSuccessfulAttemptTime *string
	// Informative message about the latest log delivery attempt. If the log
	// delivery fails with USER_FAILURE, error details will be provided for fixing
	// misconfigurations in cloud permissions.
	Message *string
}

// * Update Log Delivery Configuration.
type UpdateLogDeliveryConfigurationRequest struct {
	// The log delivery configuration id of customer
	ConfigId *string
	// <Databricks> account ID of any type. For non-E2 account types, get your
	// account ID from the [Accounts Console].
	//
	// [Accounts Console]: https://docs.databricks.com/administration-guide/account-settings/usage.html
	AccountId *string
	// Status of log delivery configuration. Set to `ENABLED` (enabled) or
	// `DISABLED` (disabled). Defaults to `ENABLED`. You can [enable or disable the
	// configuration](#operation/patch-log-delivery-config-status) later. Deletion
	// of a configuration is not supported, so disable a log delivery configuration
	// that is no longer needed.
	Status LogDeliveryConfigStatus
}

type UpdateLogDeliveryConfigurationResponse struct {
}
